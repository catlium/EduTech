[project]
name = "catlium-ocr"
version = "0.0.1"
description = "CatLium EduTech - OCR Service (FastAPI)"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.34.0",
    "pydantic>=2.11.0",
    "pydantic-settings>=2.9.0",
    "httpx>=0.28.0",
    "python-multipart>=0.0.12",
    "numpy>=1.26.0",
    "pillow>=10.0.0",
    "pymupdf>=1.24.0",
    # Local OCR engine (CPU build). PaddleOCR is a document component, not an
    # LLM; it runs entirely on-device. GPU builds swap `paddlepaddle` for
    # `paddlepaddle-gpu` at deploy time.
    "paddlepaddle>=3.0.0",
    "paddleocr>=3.0.0",
]

[project.optional-dependencies]
dev = [
    "ruff>=0.11.0",
    "mypy>=1.15.0",
    "pytest>=8.3.0",
    "pytest-asyncio>=0.25.0",
]

[build-system]
requires = ["setuptools>=75.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["app*"]

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "N", "UP", "B", "A", "C4", "SIM", "TCH"]

[tool.mypy]
python_version = "3.12"
strict = true
warn_return_any = true
warn_unused_configs = true

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
