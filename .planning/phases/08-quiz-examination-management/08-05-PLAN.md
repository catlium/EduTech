---
phase: 08-quiz-examination-management
plan: 05
title: "Gap closure — publish gate excludes ARCHIVED questions (WR-03, EXAM-08)"
status: planned
date: 2026-09-05
type: execute
wave: 1
autonomous: true
depends_on: ["08-04"]
gap_closure: true
files_modified:
  - apps/api/src/examinations/examinations.service.ts
  - docs/api/assessments.md
  - /tmp/opencode/p8_e2e.sh
  - docs/user-validation.md
requirements: [EXAM-08]

estimate:
  tokens: 26000
  raw_tokens: 13000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "docs/api/assessments.md is verified against implemented behavior (CON-01-style sweep; discrepancies fixed deliberately, not hidden) — the :269 claim that ARCHIVED questions fail publish validation now matches the implemented gate (VERIFICATION.md gap 1 / failed truth #21)."
    - "Publish gate: ≥1 question, all linked currently APPROVED and ACTIVE, duration > 0, maxMarks > 0, valid schedule — an ARCHIVED+APPROVED question blocks publish with 400 (gap missing item: add `|| q.question.status !== 'ACTIVE'` to the unapproved filter)."
    - "An ARCHIVED (non-ACTIVE) question cannot be linked to an assessment at all — addQuestions rejects ARCHIVED status with 400 (new behavioral decision, defense in depth with the publish gate)."
    - "PENDING/REJECTED/ARCHIVED linked question blocks publish with a 400 that names the unapproved/unACTIVE count; message contains BOTH 'not APPROVED' and 'not ACTIVE' substrings."
  artifacts:
    - path: "apps/api/src/examinations/examinations.service.ts"
      provides: "publishAssessment gate filters on approvalStatus AND status; addQuestions blocks ARCHIVED links"
    - path: "docs/api/assessments.md"
      provides: "publish + add-questions sections + negative-rules :269 state the ACTIVE requirement truthfully"
    - path: "/tmp/opencode/p8_e2e.sh"
      provides: "EXAM-08 block extended with ARCHIVED gate cases (link-time 400, archive-after-link publish 400, reactivate publish 201)"
    - path: "docs/user-validation.md"
      provides: "EXAM-08 case records the ARCHIVED gate checks"
  key_links:
    - from: "publishAssessment unapproved filter"
      to: "linked questions' CURRENT approvalStatus AND status"
      via: "listQuestions JOIN rows re-checked at publish (Pitfall 1), filter `q.question.approvalStatus !== 'APPROVED' || q.question.status !== 'ACTIVE'`"
    - from: "addQuestions per-id check"
      to: "questions.status ACTIVE gate"
      via: "per-id select extended to status; non-ACTIVE question → 400 before insert"
---

<objective>
Close the failed verification truth (WR-03): the publish gate for EXAM-08
checks only `approvalStatus`, so an ARCHIVED+APPROVED question passes into a
PUBLISHED assessment — contradicting `docs/api/assessments.md:269`, which the
08-04 CON-02 sweep promised to keep accurate. Make the gate enforce "only
ACTIVE+APPROVED questions in official assessments" at BOTH link time
(addQuestions) and publish time (publishAssessment), fix the doc to match the
implemented behavior, and extend the E2E harness + `docs/user-validation.md`
EXAM-08 case with ARCHIVED-gate checks.

Purpose: EXAM-08 ("Only APPROVED questions usable in official assessments") is
the phase's weakest gate — `question.status` (ACTIVE/ARCHIVED) is independent
of `approvalStatus` (archiving changes status only), so the documented contract
promise and the code diverge. Closing this restores the CON-02 sweep's core
promise (discrepancies fixed deliberately, not hidden).
Output: Gate + doc + E2E all aligned: ARCHIVED questions are unlinkable and
unpublishable; the suite stays green.
</objective>

<execution_context>
@/home/ashif/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/ashif/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-quiz-examination-management/08-VERIFICATION.md (gap 1 — authoritative truth + missing items)
@.planning/phases/08-quiz-examination-management/08-REVIEW.md (WR-03 :139-158 — the exact filter fix)
@.planning/phases/08-quiz-examination-management/08-RESEARCH.md (Pitfall 1 :269-275 — re-check CURRENT question state at publish, not link time)
Prior plan: `.planning/phases/08-quiz-examination-management/08-04-SUMMARY.md` (E2E harness location `/tmp/opencode/p8_e2e.sh`, PASS=56 baseline, EXAM-08 correction history)
</context>

### Multi-Source Coverage Audit (Plan 08-05)

| Source item | Where covered |
|-------------|---------------|
| VERIFICATION gap 1 (failed): publish gate ignores ARCHIVED status | T1 (filter fix), T2 (E2E cases) |
| VERIFICATION gap 1 missing: `\|\| q.question.status !== 'ACTIVE'` on the unapproved filter | T1 (exact filter) |
| VERIFICATION gap 1 missing: re-run publish-gate E2E with ARCHIVED+APPROVED (expect 400) + update user-validation EXAM-08 | T2 |
| VERIFICATION gap 1 option: decide whether addQuestions also blocks ARCHIVED links | T1 (DECISION: block — assumption record) |
| REQ EXAM-08: Only APPROVED questions in official assessments | T1/T2 (ACTIVE+APPROVED composition) |
| RESEARCH Pitfall 1: re-check CURRENT status at publish, not link time | T1 (filter re-queries linked rows at publish) |
| REVIEW WR-03 fix template (:155-157) | T1 (exact filter shape) |
| CONTEXT: no discuss-phase for gap closure | n/a — VERIFICATION gaps are the acceptance contract |

Exclusions: WR-06 (answer-key exposure) — explicitly deferred to Phase 9 (VERIFICATION.md deferred block); must NOT be planned here.

<tasks>

<task type="auto">
  <name>Task 1: Publish gate filters ACTIVE status + addQuestions blocks ARCHIVED links (EXAM-08)</name>
  <files>
    apps/api/src/examinations/examinations.service.ts
    docs/api/assessments.md
  </files>
  <read_first>
    .planning/phases/08-quiz-examination-management/08-VERIFICATION.md gap 1 (failed truth: docs/api/assessments.md:269 vs publishAssessment:167)
    .planning/phases/08-quiz-examination-management/08-REVIEW.md WR-03 (:139-158 — the exact filter replacement)
    apps/api/src/examinations/examinations.service.ts:155-185 (publishAssessment gate), :247-292 (addQuestions per-id check)
    packages/database/src/schema/questions.ts:32-33 (status varchar ACTIVE/ARCHIVED independent of approvalStatus)
    apps/api/src/questions/questions.controller.ts:211-229 (archive/activate endpoints the E2E fixtures use)
    docs/api/assessments.md:187-199 (add-questions section), :264-271 (negative rules)
  </read_first>
  <action>
    1. In `publishAssessment` (examinations.service.ts:167) replace the unapproved filter with the WR-03 fix:
       `const unapproved = linked.filter((q) => q.question.approvalStatus !== 'APPROVED' || q.question.status !== 'ACTIVE');`
       and change the 400 message to `${unapproved.length} question(s) are not APPROVED or not ACTIVE`.
       Rationale: the message MUST keep the 'not APPROVED' substring (existing p8_e2e.sh:103 grep on the PENDING case still matches) AND add 'not ACTIVE' (new grep for the ARCHIVED case). The filter still counts PENDING/REJECTED questions via the first clause.
    2. Behavioral decision (NEW — record in the task summary as an assumption): `addQuestions` now also blocks ARCHIVED links — a question whose `status !== 'ACTIVE'` returns 400. This is defense in depth: the docs already promise ARCHIVED questions fail validation, and linking a question that can never be published is a teacher-facing trap. Implementation: extend the per-id Pitfall-3 select in addQuestions (currently selects only `{ id: questions.id }` at :259-263) to also select `status`; after the existing existence check (foreign question → keep the exact current 400 'Question ${id} not found or not in this institute'), add `if (question.status !== 'ACTIVE') throw new BadRequestException('Question ${id} is not ACTIVE')`. Do NOT reorder: existence (institute scope) is checked first, status second — cross-tenant 404/400 semantics (Pitfall 3) must be unchanged.
    3. `docs/api/assessments.md` edits to make the doc match the new behavior:
       - Publish section (:218-222): the gate line gains the ACTIVE requirement — every linked question must be `APPROVED` (EXAM-08) and `ACTIVE` (ARCHIVED questions fail validation); approval and status are re-checked at publish time, not link time.
       - Negative rules :269: keep the exact 'PENDING/REJECTED/ARCHIVED questions fail validation' claim — it becomes TRUE after this task (it was the lie this gap closes); optionally append '(a question must be ACTIVE and APPROVED)'.
       - Add-questions section (:193-199): add one sentence — an ARCHIVED (non-ACTIVE) questionId returns `400` (`Question ... is not ACTIVE`); foreign-institute behavior unchanged.
    Do NOT touch any other exam module behavior, do NOT create a migration, do NOT alter contracts (no schema/contract work needed — varchar status already carries ACTIVE/ARCHIVED).
  </action>
  <acceptance_criteria>
    - publishAssessment rejects an ARCHIVED+APPROVED linked question with 400 and a message containing both 'not APPROVED' and 'not ACTIVE'.
    - addQuestions rejects an already-ARCHIVED question with 400 'Question ... is not ACTIVE'; foreign-institute questionId still returns the exact existing 400; the transactional insert path is otherwise unchanged.
    - docs/api/assessments.md publish + add-questions + negative-rules sections state the ACTIVE requirement; the :269 ARCHIVED claim now matches runtime behavior.
    - `pnpm typecheck && pnpm lint` pass (run from repo root).
  </acceptance_criteria>
  <done>The publish gate fails ARCHIVED+APPROVED questions with 400 (filter is `approvalStatus !== 'APPROVED' || status !== 'ACTIVE'`), addQuestions blocks non-ACTIVE questions, and docs/api/assessments.md matches both behaviors.</done>
  <verify>
    <automated>
      pnpm typecheck && pnpm lint
      # static proof the filter changed (grep the exact edited line)
      grep -n "approvalStatus !== 'APPROVED' || q.question.status !== 'ACTIVE'" apps/api/src/examinations/examinations.service.ts
      grep -n "is not ACTIVE" apps/api/src/examinations/examinations.service.ts
      grep -n "not APPROVED or not ACTIVE" apps/api/src/examinations/examinations.service.ts
    </automated>
    <fails_when>
      - The publish filter still checks approvalStatus only (no status clause) — gate remains bypassable by ARCHIVED+APPROVED.
      - The publish 400 message no longer contains 'not APPROVED' (breaks the existing E2E grep at p8_e2e.sh:103).
      - addQuestions rejects a foreign-institute question with the 'is not ACTIVE' message instead of the unchanged Pitfall-3 400.
      - typecheck/lint fail.
    </fails_when>
  </verify>
</task>

<task type="auto">
  <name>Task 2: E2E — ARCHIVED gate cases in p8_e2e.sh + docs/user-validation.md EXAM-08 update (EXAM-08)</name>
  <files>
    /tmp/opencode/p8_e2e.sh
    docs/user-validation.md
  </files>
  <read_first>
    /tmp/opencode/p8_e2e.sh (whole harness — 135 lines; EXAM-08 block :99-105, fixtures :28-36, ok/req helpers :16-25)
    .planning/phases/08-quiz-examination-management/08-04-SUMMARY.md (:127 — the documented harness double-execution + EXAM-08 positive-case correction)
    docs/user-validation.md:660-671 (EXAM-08 case — the section this task extends)
    docs/user-validation.md:531-559 (fixtures block — cookie/account re-issue instructions)
    apps/api/src/questions/questions.controller.ts:211-229 (archive/activate endpoints used by the new cases)
  </read_first>
  <action>
    HARNESS BASELINE FIRST: the E2E run is mutation-heavy and fixture cookies go stale (08-04-SUMMARY). Re-issue teacher-A/student/B cookies + fixtures per docs/user-validation.md fixtures block, then run `/tmp/opencode/p8_e2e.sh` fresh. It MUST report FAIL=0 (documented baseline PASS=56, 2026-09-05).
    KNOWN HARNESS FLAW TO REPAIR IF PRESENT: the current EXAM-08 positive case (line 104-105) links QA to A8 which ALREADY holds the PENDING QP, so publish cannot return 201. This is the pre-correction state the 08-04-SUMMARY documents as fixed ('4 corrected EXAM-08 gate cases' — the positive case uses a SEPARATE assessment). If the fresh run reports FAIL=1 at that check, apply the documented correction: create a fresh assessment A8b with only QA linked, publish → 201, assert status PUBLISHED — restoring the 56/0 baseline BEFORE adding gap cases. Record the repair in the task summary.
    EXTEND THE EXAM-08 BLOCK with exactly 4 new checks (each counts +1 PASS via the ok/grep helpers):
    1. Link-time gate: create a fresh MANUAL MCQ fixture QE (auto-APPROVED, ACTIVE), POST /questions/:id/archive → 200 status ARCHIVED, then POST a fresh DRAFT assessment A9 with {questionIds:[QE]} → `ok "$ST" 400 "addQuestions with ARCHIVED question -> 400"` + grep message 'is not ACTIVE' (+1 PASS).
    2. Publish-time gate (defense in depth — the VERIFICATION human-check scenario): POST another fresh DRAFT A10, link the ACTIVE+APPROVED QA, then POST /questions/:id/archive on QA (QA becomes ARCHIVED while linked), POST A10/publish → `ok "$ST" 400 "publish with ARCHIVED+APPROVED question -> 400"` and grep the body for 'not ACTIVE' (+2 PASS — one ok + one grep).
    3. Current-status re-check round-trip (Pitfall 1): POST /questions/:id/activate on QA (back to ACTIVE), POST A10/publish → `ok "$ST" 201 "publish after reactivation -> 201"` (+1 PASS).
    End-of-case hygiene: reactivate QE too (leave the fixture pool ACTIVE) so later sections / 08-06 / 08-07 cases are not polluted. Do NOT reuse A8's id in the new cases; keep the PENDING-gate case untouched.
    UPDATE docs/user-validation.md EXAM-08 section (:660-671): add the ARCHIVED sub-cases to Expected output — addQuestions with an ARCHIVED question → 400 'is not ACTIVE'; publish with an ARCHIVED+APPROVED question (archived after linking) → 400 'not APPROVED or not ACTIVE'; reactivating restores publishability → 201. Keep every existing marker [x] (file must stay green — no [ ], no [!]).
    RUN the extended suite once more; the final tally MUST be PASS=60 FAIL=0 (56 baseline + 4 new). CRITICAL: do not add any check beyond the 4 enumerated unless you also update this count — the fails_when gate asserts the exact number.
  </action>
  <acceptance_criteria>
    - The suite runs FAIL=0 at baseline (harness flaw repaired per 08-04-SUMMARY if present) and FAIL=0 with the 4 new ARCHIVED checks → final PASS=60.
    - The 4 new checks prove: ARCHIVED link → 400; ARCHIVED+APPROVED (archived after linking) publish → 400 with 'not ACTIVE' in the message; reactivated question publish → 201.
    - docs/user-validation.md EXAM-08 case records the ARCHIVED sub-cases; zero unchecked markers in the file.
  </acceptance_criteria>
  <done>p8_e2e.sh reports PASS=60 FAIL=0 with the EXAM-08 block proving ARCHIVED questions are rejected at both link time and publish time, and docs/user-validation.md EXAM-08 documents the ARCHIVED cases with all markers green.</done>
  <verify>
    <automated>
      bash /tmp/opencode/p8_e2e.sh 2>&1 | tail -5
      # the tail MUST read: RESULT: PASS=60 FAIL=0
      bash /tmp/opencode/p8_e2e.sh 2>&1 | grep -c "ARCHIVED+APPROVED question -> 400"
      # must be 1 (the publish-time case ran)
    </automated>
    <fails_when>
      - The suite reports FAIL≠0 or PASS≠60 (missing/misimplemented gate case, or count drift from unplanned extra checks).
      - The publish-with-ARCHIVED+APPROVED case does not return 400 (gate still vulnerable).
      - docs/user-validation.md EXAM-08 gains any [ ] or [!] marker.
    </fails_when>
    <human-check>
      Spot-confirm the live output of case "publish with ARCHIVED+APPROVED question -> 400" in the recorded suite log (the E2E harness output in the task summary) — the VERIFICATION human-check item 1 maps to this automated case.
    </human-check>
  </verify>
</task>

</tasks>

### Artifacts this plan produces (gap-closure scope)

- `ExaminationsService.publishAssessment` — unapproved filter extended with `|| q.question.status !== 'ACTIVE'`; message names both conditions
- `ExaminationsService.addQuestions` — per-id select extended to `{ id, status }`; non-ACTIVE question → 400 'Question ... is not ACTIVE'
- `docs/api/assessments.md` — publish/add-questions/negative-rules match the implemented ACTIVE requirement
- `/tmp/opencode/p8_e2e.sh` — EXAM-08 block extended (4 checks) → suite PASS=60
- `docs/user-validation.md` — EXAM-08 case records the ARCHIVED gate checks

### Assumptions recorded (assumption-delta contribution)

- **A-08-G1 (NEW behavioral decision):** ARCHIVED (non-ACTIVE) questions are unlinkable at addQuestions AND unpublishable at publishAssessment. The alternative from VERIFICATION gap 1 — allow ARCHIVED links and rely solely on the publish gate, or correct the doc to 'PENDING/REJECTED' — was rejected: the docs already promise ARCHIVED fails validation, and a teacher linking a doomed question is a trap. Blocking at link time keeps the assessment assemblable-from-valid-questions guarantee.
- **A-08-G2:** WR-06 (answer-key exposure) remains deferred to Phase 9 per the VERIFICATION deferred block — not planned here.

<threat_model>
**ASVS level 1 — block on HIGH.**

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API (question linking) | questionIds + question status cross here; addQuestions decides linkability server-side |
| client→API (publish) | the EXAM-08 gate crosses here; publishability decided from CURRENT DB state, never client claims |
| API→PostgreSQL | filter/status values become SQL; the questions.status column is read (not written) by this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-20 | Tampering | publish gate bypass via ARCHIVED+APPROVED question | high | mitigate | publishAssessment filter `approvalStatus !== 'APPROVED' || status !== 'ACTIVE'` re-queries CURRENT linked rows (Pitfall 1); E2E proves archive-after-link → publish 400 and reactivate → publish 201 |
| T-08-21 | Tampering | ARCHIVED question linked during assembly | medium | mitigate | addQuestions per-id select extended to status; non-ACTIVE → 400 before insert; E2E proves ARCHIVED link → 400 |
| T-08-22 | Information Disclosure | cross-institute question link semantics changed | low | mitigate | existence (institute-scope) check runs FIRST and keeps the exact Pitfall-3 400; the status check only fires for in-institute questions — no new oracle |
| T-08-SC | Tampering | npm/pip/cargo installs | low | accept | No installs in this plan (08-RESEARCH.md Package Legitimacy Audit: none); dependency surface unchanged |
</threat_model>

<verification>
- `pnpm typecheck && pnpm lint` green after Task 1.
- E2E: baseline FAIL=0 (harness flaw repaired per documented 08-04 correction if present), then final PASS=60 FAIL=0 with the 4 new ARCHIVED gate checks.
- docs/api/assessments.md publish/add-questions/negative-rules statements verified against the implemented gate (grep checks in Task 1).
</verification>

<success_criteria>
- An ARCHIVED+APPROVED question can no longer enter a PUBLISHED assessment: blocked at link time (400) and at publish time (400 with 'not ACTIVE' named).
- docs/api/assessments.md:269's ARCHIVED claim is true against runtime behavior — the CON-02 sweep's core promise is restored.
- The E2E suite + docs/user-validation.md EXAM-08 case cover the ARCHIVED edge; the suite is green at PASS=60 FAIL=0.
</success_criteria>

<output>
Create `.planning/phases/08-quiz-examination-management/08-05-SUMMARY.md` when done
</output>