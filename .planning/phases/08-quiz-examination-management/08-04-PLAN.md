---
phase: 08-quiz-examination-management
plan: 04
title: E2E validation checklist, task tracking docs, and phase close
status: planned
date: 2026-09-04
type: execute
wave: 4
autonomous: true
depends_on: ["08-03"]
files_modified:
  - docs/user-validation.md
  - docs/tasks.md
  - docs/project-status.md
  - docs/api/assessments.md
requirements: [EXAM-01, EXAM-02, EXAM-03, EXAM-04, EXAM-05, EXAM-06, EXAM-07, EXAM-08]
requirements_addressed: [EXAM-01, EXAM-02, EXAM-03, EXAM-04, EXAM-05, EXAM-06, EXAM-07, EXAM-08]

estimate:
  tokens: 32000
  raw_tokens: 16000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - docs/user-validation.md has a Phase 8 section covering EXAM-01..08 (setup + per-req tests + negative/security cases) and every item is marked [x] — the E2E run passed against the live stack.
    - docs/api/assessments.md is verified against implemented behavior (CON-01-style sweep; discrepancies fixed deliberately, not hidden).
    - docs/tasks.md contains the Phase 8 block with all EXAM-01..08 items [x]; docs/project-status.md has the Phase 8 checkpoint entry per AGENTS.md Rules 3/4.
    - Final pnpm typecheck && pnpm lint pass; the phase's meaningful checkpoint is committed (and pushed when a remote exists).
  artifacts:
    - docs/user-validation.md (Phase 8 section)
    - docs/tasks.md (Phase 8 block)
    - docs/project-status.md (Phase 8 entry)
    - docs/api/assessments.md (verified/corrected)
  key_links:
    - Checklist payloads must be named from the Zod contract/DTO (Pitfall: field-name drift caused issues in Phase 5/6).
    - Fixture membership status stays lowercase 'active' (Pitfall 3 from Phase 6).
    - The lifecycle items (EXAM-06) must each run in order DRAFT→PUBLISHED→ACTIVE→COMPLETED to prove the reachable path.
---

<objective>
Close the phase with the repo's validation architecture: the E2E checklist in
docs/user-validation.md (platform standard — zero automated tests exist by
design), all items run and marked [x] against the dockerized stack; the AGENTS.md
checkpoint protocol docs (docs/tasks.md, docs/project-status.md); and a
contract-vs-behavior sweep of docs/api/assessments.md (CON-01 style, Phase 5/6
precedent).

Purpose: Every EXAM requirement gets a runnable validation trail, and a fresh
session can recover the phase state from docs alone (AGENTS.md Rule 6-7).
Output: Closed phase with a committed checkpoint and all validation items green.
</objective>

<execution_context>
@/home/ashif/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/ashif/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-quiz-examination-management/08-RESEARCH.md (Validation Architecture section :631-664 — the test map is the checklist skeleton)
Prior plan SUMMARYs: `.planning/phases/08-quiz-examination-management/08-01-SUMMARY.md`, `08-02-SUMMARY.md`, `08-03-SUMMARY.md` (implemented surface + sweep results this plan documents; read before writing the checklist)
</context>

### Multi-Source Coverage Audit (Plan 08-04)

| Source item | Where covered |
|-------------|---------------|
| GOAL: Assessment management works with enforced state transitions | T1 (full E2E trail documenting every EXAM behavior), T2 (contract sweep + close) |
| REQ EXAM-01..08 | T1 (one checklist item per EXAM-01..08 including negative/security cases) |
| RESEARCH: Phase Requirements → Test Map (:641-652) | T1 (the E2E skeleton) |
| CONTEXT: none (no discuss-phase) | n/a |

Exclusions: student attempts (Phase 9), evaluation (Phase 10), results (Phase 11), analytics (Phase 12), frontend — out of scope per REQUIREMENTS.md/ROADMAP.

<tasks>

<task type="auto">
  <name>Task 1: Write the Phase 8 E2E checklist in docs/user-validation.md and run it to all [x]</name>
  <files>docs/user-validation.md</files>
  <read_first>
    docs/user-validation.md:1-13 (item format) and the Phase 6/7 sections (fixtures to reuse: institute 11111111-..., membership 22222222-... lowercase 'active', subject slug 'math', teacher/student register/login endpoints)
    .planning/phases/08-quiz-examination-management/08-RESEARCH.md:641-652 (Phase Requirements → Test Map: the eight EXAM behaviors + their E2E checks)
    .planning/phases/08-quiz-examination-management/08-RESEARCH.md Pitfalls 1/2/3/6 (approval re-check, state-guarded edits, cross-tenant linking, schedule validation) and Pattern 4 (schedule check on read)
    .planning/phases/08-quiz-examination-management/08-01-SUMMARY.md, 08-02-SUMMARY.md, 08-03-SUMMARY.md (actual endpoints + behavior + sweep results to encode)
  </read_first>
  <action>
    Append a "## Phase 8 — Quiz & Examination Management (E2E)" section to docs/user-validation.md following the established format. Structure:
    - Setup prerequisites: reuse the live Phase 6/7 fixtures where present (institute 11111111-..., membership 22222222-... status lowercase 'active', academic scope from `SELECT id FROM subjects WHERE slug='math'`); register fresh teacher + student users, bind memberships via SQL; create APPROVED questions via POST /questions (source MANUAL) and a PENDING question (source AI_GENERATED). State the casing requirement explicitly.
    - One item per requirement, each with the format fields (Setup required / Endpoint / Payload / Expected output), payloads named VERBATIM from the CreateAssessmentDto/contract field names (title/description/durationMinutes/maxMarks/instructions/startsAt/endsAt/status — no invented names):
      - EXAM-01: create → 201 + status DRAFT; list → array + questionCount; retrieve → 200; PATCH → 200; DELETE → 204 then GET → 404.
      - EXAM-02: POST :id/questions adds APPROVED questions → 200; DELETE :id/questions/:qid → 204; GET :id/questions lists them.
      - EXAM-03: create with durationMinutes + maxMarks + instructions → echo both in response; PATCH updates them.
      - EXAM-04: create with startsAt+endsAt; invalid schedule (endsAt before startsAt) → 400.
      - EXAM-05: publish → 200 PUBLISHED; complete → 200 COMPLETED (from ACTIVE).
      - EXAM-06: lifecycle DRAFT→PUBLISHED→ACTIVE→COMPLETED run in order, each status confirmed.
      - EXAM-07: illegal transitions (DRAFT→ACTIVE, ACTIVE→DRAFT, complete-from-DRAFT, any-on-COMPLETED) → 400 each.
      - EXAM-08: publish a DRAFT holding a PENDING question → 400 (approved-only gate); publish with all APPROVED → 200.
      - Security/negative block: status/instituteId in body → 400; cross-institute (institute-B member) GET/PATCH/DELETE/publish/complete of institute-A assessment → 404; STUDENT create/mutate/transition → 403, reads → 200; random uuid → 404; no cookie → 401; non-member institute header → 403 (Phase 5 finding: TenantGuard 403, NOT 404 — encode the CORRECT expectation).
    - Run every item against the live dockerized stack and mark each [x] with the run date. Any [!] must be diagnosed and fixed before the item can flip to [x] — no deferred items for a phase close.
    - If any behavior contradicts docs/api/assessments.md, record the discrepancy for Task 2's deliberate sweep (CON-02), do NOT silently edit the contract here.
  </action>
  <acceptance_criteria>
    - docs/user-validation.md contains "## Phase 8 — Quiz & Examination Management (E2E)" with items covering EXAM-01..08 plus the security/negative block.
    - Every Phase 8 item is marked [x]; the section header carries a passing run date.
    - The checklist's payload field names match the contract exactly (no drift).
    - Any doc-vs-behavior discrepancy found is logged for Task 2.
  </acceptance_criteria>
  <done>docs/user-validation.md has a dated Phase 8 E2E section with all EXAM-01..08 items plus the security/negative block marked [x], payload names matching the contract exact, and any doc-vs-behavior discrepancy logged.</done>
  <verify>
    <automated>
      pnpm typecheck
      # all phase-8 items checked (no [ ], no [!] remain inside the Phase 8 section)
      awk '/## Phase 8 — Quiz & Examination Management/,0' docs/user-validation.md | grep -v '^#' | grep -cE '\[ \]|\[!\]' | grep -w 0
      # key items exist
      grep -q 'assessments' docs/user-validation.md
      grep -q 'PUBLISHED' docs/user-validation.md
      grep -q 'COMPLETED' docs/user-validation.md
      grep -q 'APPROVED' docs/user-validation.md
      grep -q 'STUDENT' docs/user-validation.md
    </automated>
    <fails_when>
      - awk-grep counts any [ ] or [!] marker in the Phase 8 section (item not run/failed — phase cannot close with an unchecked item).
      - The EXAM-06 lifecycle or EXAM-07 transition items are absent (the core state-machine behaviors never validated).
      - typecheck fails.
    </fails_when>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Update docs/tasks.md + docs/project-status.md, sweep docs/api/assessments.md vs behavior, final gate, commit checkpoint</name>
  <files>
    docs/tasks.md
    docs/project-status.md
    docs/api/assessments.md (only if the Task 1 discrepancy log found mismatches)
  </files>
  <read_first>
    docs/tasks.md (existing Phase 7 block — same format: Phase → Goal → task checkboxes with markers [ ]/[~]/[x]/[-]/[!])
    docs/project-status.md (existing checkpoint format — AGENTS.md Rule 3 fields)
    .planning/phases/08-quiz-examination-management/08-01-PLAN.md/08-02-PLAN.md/08-03-PLAN.md verification sections (what each task proved, for the validation-status line)
  </read_first>
  <action>
    1. docs/tasks.md — add the "Phase 8 — Quiz & Examination Management" block per AGENTS.md Rule 4 listing the eight EXAM requirements as checkboxes and mark each [x]; keep Phase 7's statuses untouched.
    2. docs/project-status.md — add/replace the Phase 8 entry per AGENTS.md Rule 3: status "COMPLETE (E2E validated 2026-09-04)", completed work (examinations module, migration 0008, contracts, assessments + assessment_questions tables, CRUD + state machine + question linking), pending (Phase 9), deferred (none), DB changes (assessments + assessment_questions tables + unique constraint), validation status (all user-validation Phase 8 items [x]), latest checkpoint (this task's commit), recommended next task (plan Phase 9 — Student Examination Attempts).
    3. Contract sweep (CON-01 style): re-read docs/api/assessments.md from 08-01 and diff it against the ACTUAL implemented behavior (routes, status codes, field names, error semantics, transition legal/illegal sets — pull from the 08-01/08-02/08-03 SUMMARYs and the user-validation run). Fix every mismatch in docs/api/assessments.md deliberately (e.g., confirm the activate + unpublish endpoints are documented, and that addQuestions/removeQuestion/PATCH are documented as DRAFT-only) and note each fix in the SUMMARY.
    4. Final gate: run `pnpm typecheck && pnpm lint` from the repo root; both green.
    5. Commit per AGENTS.md Rule 8/10: meaningful checkpoint covering the docs + any doc fixes (docs(phase8): close exam management phase with E2E validation — or feat(examinations): ... if code fixes were required during Task 1). Push when a remote is available (Rule 9); record the commit hash.
  </action>
  <acceptance_criteria>
    - docs/tasks.md Phase 8 block lists EXAM-01..08 all marked [x]; other phases' entries unmodified.
    - docs/project-status.md Phase 8 entry exists with all Rule-3 fields.
    - docs/api/assessments.md matches implemented behavior after the sweep — no knowingly-wrong statement left.
    - pnpm typecheck && pnpm lint pass; checkpoint committed (and pushed if a remote exists).
  </acceptance_criteria>
  <done>docs/tasks.md (EXAM-01..08 [x]) and docs/project-status.md (Phase 8 COMPLETE entry) updated; docs/api/assessments.md swept against behavior; final typecheck+lint green; the phase-close checkpoint is committed and pushed when a remote exists.</done>
  <verify>
    <automated>
      pnpm typecheck && pnpm lint
      # tasks.md Phase 8 block: all eight EXAM items checked
      awk '/Phase 8 — Quiz & Examination Management/,0' docs/tasks.md | grep -v '^#' | grep -cE '\[ \]|\[~\]|\[!\]|\[-\]' | grep -w 0
      # project-status has the Phase 8 entry with validation evidence
      grep -q 'EXAM-01' docs/tasks.md
      grep -q 'COMPLETE' docs/project-status.md
      # phase-8 user-validation section still fully green after any edits
      awk '/## Phase 8 — Quiz & Examination Management/,0' docs/user-validation.md | grep -v '^#' | grep -cE '\[ \]|\[!\]' | grep -w 0
      git log --oneline -1
    </automated>
    <fails_when>
      - Any EXAM item in tasks.md is not [x], or project-status lacks the Phase 8 entry.
      - typecheck/lint fail.
      - The user-validation Phase 8 section regressed (an awk count above 0).
      - No new commit exists for the phase close.
    </fails_when>
  </verify>
</task>

</tasks>

### Artifacts this phase produces (Plan 08-04 scope)

- `docs/user-validation.md` — "## Phase 8 — Quiz & Examination Management (E2E)" section (EXAM-01..08 items + security/negative block, all [x])
- `docs/tasks.md` — Phase 8 block (EXAM-01..08 [x])
- `docs/project-status.md` — Phase 8 COMPLETE entry (Rule-3 fields)
- `docs/api/assessments.md` — verified/corrected to match implemented behavior

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → future sessions/Phase 15 verification | wrong documentation becomes wrong contracts; a doc claiming status is client-settable or that transitions are unguarded actively undermines the security posture |
| checklist → phase-close | an unchecked [!] item closed anyway hides a real defect (Phase 5/6 precedent of drift caught only at E2E close) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-20 | Repudiation | docs/user-validation.md phase close | medium | mitigate | Phase 8 section may only close with every item [x]; awk gate fails on any [ ]/[!]; run date-stamped in the section header (Phase 5/6 precedent) |
| T-08-21 | Tampering | docs/api/assessments.md contract integrity | medium | mitigate | CON-01-style sweep in Task 2 diffs the doc against implemented routes/status codes/field names/transition sets; discrepancies fixed deliberately and logged (CON-02), never silently edited during Task 1 |
| T-08-22 | Information Disclosure | checklist credentials/fixtures | low | accept | The doc only carries dummy fixture UUIDs + throwaway test accounts (established Phase 5-7 pattern); no real credentials/tokens recorded |
| T-08-SC | Tampering | npm/pip/cargo installs | low | accept | No installs in this plan; dependency surface unchanged across the phase (08-RESEARCH.md Package Legitimacy Audit) |
</threat_model>

<verification>
- Phase 8 user-validation section fully [x]; tasks.md EXAM block fully [x]; project-status Phase 8 COMPLETE entry present.
- docs/api/assessments.md swept against behavior with fixes logged.
- Final `pnpm typecheck && pnpm lint` green; checkpoint committed (+ pushed when remote exists).
</verification>

<success_criteria>
- Every EXAM-01..08 requirement has a passed, dated E2E validation item in docs/user-validation.md — the phase's observable proof.
- A fresh session can recover Phase 8 state and the exact next task (Phase 9 planning) from docs alone.
- Phase 8 is closed per AGENTS.md checkpoint protocol with the commit pushed.
</success_criteria>

<output>
Create `.planning/phases/08-quiz-examination-management/08-04-SUMMARY.md` when done
</output>
