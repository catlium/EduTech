---
phase: 08-quiz-examination-management
plan: 01
title: Assessment schema, migration 0008, contracts, and create/get/list tracer slice
status: planned
date: 2026-09-04
type: execute
wave: 1
autonomous: true
depends_on: []
files_modified:
  - packages/database/src/schema/examinations.ts (new)
  - packages/database/src/schema/index.ts
  - packages/database/src/index.ts
  - packages/database/drizzle/0008_*.sql (generated)
  - packages/contracts/src/index.ts
  - apps/api/src/examinations/examinations.module.ts (new)
  - apps/api/src/examinations/examinations.controller.ts (new)
  - apps/api/src/examinations/examinations.service.ts (new)
  - apps/api/src/examinations/dto/create-assessment.dto.ts (new)
  - apps/api/src/examinations/dto/assessment-query.dto.ts (new)
  - apps/api/src/app/app.module.ts
requirements: [EXAM-01, EXAM-03, EXAM-04]
requirements_addressed: [EXAM-01, EXAM-03, EXAM-04]

estimate:
  tokens: 56000
  raw_tokens: 28000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - The assessments and assessment_questions tables exist in PostgreSQL (migration 0008 generated via drizzle-kit generate and applied cleanly; no drizzle-kit push).
    - POST /api/v1/assessments creates an assessment with status DRAFT by default, from a TEACHER/INSTITUTE_ADMIN only, accepting title, description, durationMinutes, maxMarks, instructions (JSONB), startsAt, endsAt — and returns 201.
    - GET /api/v1/assessments/:assessmentId returns 200 for an in-institute id and 404 for a foreign/nonexistent id (tenant-scoped, anti-IDOR).
    - GET /api/v1/assessments returns the institute's assessments ordered by updatedAt desc, each bearing a computed questionCount.
    - Request bodies containing instituteId are rejected with 400 (whitelist mass-assignment hardening); status is never accepted from a body (always DRAFT on create).
    - Schedule validation is enforced server-side: startsAt must be a future/normal datetime, endsAt must be after startsAt when both are provided.
    - pnpm typecheck && pnpm lint pass.
  artifacts:
    - packages/database/src/schema/examinations.ts (assessments + assessment_questions tables)
    - packages/database/drizzle/0008_*.sql
    - packages/database/src/schema/index.ts + packages/database/src/index.ts (barrel exports)
    - packages/contracts/src/index.ts (Assessment contracts section)
    - apps/api/src/examinations/examinations.module.ts, examinations.controller.ts, examinations.service.ts
    - apps/api/src/examinations/dto/create-assessment.dto.ts, assessment-query.dto.ts
    - apps/api/src/app/app.module.ts
  key_links:
    - Migration 0008 must stay in sync with the examinations schema; generate + migrate only, never drizzle-kit push.
    - Every assessments query must AND eq(assessments.instituteId, instituteId) — the anti-IDOR invariant.
    - Contract enum and DTO @IsIn lists must match exactly (field-name drift precedent from Phase 5/6).
    - Membership fixture status must be lowercase 'active' or TenantGuard rejects with 403.
    - assessment_questions.questionId FK must carry onDelete cascade (Pitfall 4 — orphan prevention); assessments.instituteId FK references institutes.id; assessments.createdBy references users.id directly (research correction, NOT questions.createdBy).
---

<objective>
Create the `examinations` module's foundation: the Drizzle schema (`assessments`
+ `assessment_questions` join table), its migration (0008), the canonical Zod
contracts, and the thinnest end-to-end slice — create an assessment (DRAFT by
default), retrieve by id, list (with computed questionCount). Duration, max
marks, instructions, and the schedule window are all captured at create time so
EXAM-03/EXAM-04 are wired from the first insert.

Purpose: Prove the full assessment stack end-to-end on one slice before expanding
to update/delete/question-linking (08-02) and the state machine (08-03). The
schema and migration establish the exact-column contract the join table and
state transitions build on.
Output: Working assessments + assessment_questions tables, migration, and
create/get/list endpoints verified against the live dockerized stack.
</objective>

<execution_context>
@/home/ashif/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/ashif/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-quiz-examination-management/08-RESEARCH.md
AGENTS.md checkpoint protocol
</context>

### Multi-Source Coverage Audit (Plan 08-01)

| Source item | Where covered |
|-------------|---------------|
| GOAL: Assessment CRUD; duration/max marks/instructions; scheduling | T1 (create/get/list + duration/maxMarks/instructions/schedule on create), 08-02 (update/delete), 08-04 (E2E close) |
| REQ EXAM-01 create/retrieve/list (+ update/delete in 08-02) | T1 (create/get/list), 08-02 (update/delete) |
| REQ EXAM-03 configure duration + max marks + instructions | T1 (columns + contracts + create insert) |
| REQ EXAM-04 scheduling | T1 (startsAt/endsAt columns + server-side schedule validation) |
| RESEARCH: zero new packages, clone questions module, varchar status (no pgEnum), JSONB instructions, generated migration 0008, createdBy→users.id direct | T1 |
| RESEARCH: Schedule check on read (no cron); schedule validated on create | T1 (server-side validation of startsAt/endsAt) |
| RESEARCH: join table assessment_questions with marks + sortOrder + questionId onDelete cascade; unique(assessmentId, questionId) | T1 (schema created now; linking used in 08-02) |
| REQ EXAM-02 add/remove questions | 08-02 (join table consumed there) |
| REQ EXAM-05 publish+complete, EXAM-06 lifecycle, EXAM-07 transitions, EXAM-08 approved-only | 08-03 (state machine) |
| CONTEXT: none exists (no discuss-phase) — no locked decisions, no deferred ideas | n/a — requirements + research govern |

Exclusions: student attempts (Phase 9), evaluation (Phase 10), analytics (Phase 12), frontend — all out of scope per REQUIREMENTS.md/ROADMAP.

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — examinations schema + migration 0008 + contracts + create/get/list slice, verified end-to-end</name>
  <files>
    packages/database/src/schema/examinations.ts (new)
    packages/database/src/schema/index.ts
    packages/database/src/index.ts
    packages/database/drizzle/0008_*.sql (generated output)
    packages/contracts/src/index.ts
    apps/api/src/examinations/dto/create-assessment.dto.ts (new)
    apps/api/src/examinations/dto/assessment-query.dto.ts (new)
    apps/api/src/examinations/examinations.module.ts (new)
    apps/api/src/examinations/examinations.controller.ts (new)
    apps/api/src/examinations/examinations.service.ts (new)
    apps/api/src/app/app.module.ts
  </files>
  <read_first>
    packages/database/src/schema/questions.ts (schema skeleton + varchar enums + FK pattern at :16-47)
    packages/database/src/schema/users.ts (users.id for the createdBy/updatedBy FK)
    packages/database/src/schema/institutes.ts (institutes.id for the instituteId FK)
    packages/database/src/schema/index.ts (barrel — append export)
    packages/database/src/index.ts (re-export list — append export)
    packages/database/drizzle.config.ts (schema glob + out dir; migrate via compose migrate, never drizzle-kit push)
    packages/database/drizzle/0007_brave_iron_monger.sql (generated migration shape to verify against)
    packages/contracts/src/index.ts lines 484-610 (Question contracts section — the exact pattern to extend: enums, payload schemas, Create/Response schemas, exported types)
    apps/api/src/questions/questions.controller.ts (guard stack, WRITE_ROLES, pipes, response wrapping — literal template)
    apps/api/src/questions/questions.service.ts (service anatomy, create/get/list, validatePayload, tenant-scoped 404)
    apps/api/src/questions/dto/question.dto.ts (DTO shape)
    apps/api/src/app/app.module.ts (module registration list)
    apps/api/src/identity/auth.controller.ts (register/login for E2E auth)
    infrastructure/compose/docker-compose.yml (migrate service :70-81, postgres creds)
    docs/user-validation.md (Phase 6/7 fixtures to reuse: institute 11111111-..., membership 22222222-... lowercase 'active', subject slug 'math')
  </read_first>
  <action>
    Build the assessment data+API slice. Do NOT use pgEnum (varchar is repo convention) and do NOT use drizzle-kit push — schema changes ship as generated migrations only (research [BLOCKING] requirement).

    1. Create packages/database/src/schema/examinations.ts with TWO tables:
       - `assessments` pgTable('assessments'): id uuid pk defaultRandom; instituteId uuid notNull references institutes.id onDelete cascade; title varchar(255) notNull; description text (nullable); durationMinutes integer (nullable); maxMarks integer (nullable); instructions jsonb (nullable); startsAt timestamp withTimezone (nullable); endsAt timestamp withTimezone (nullable); status varchar(20) notNull default 'DRAFT'; createdBy uuid notNull references users.id (research correction: reference users.id directly, NOT questions.createdBy); updatedBy uuid nullable references users.id; createdAt/updatedAt timestamp withTimezone notNull defaultNow.
       - `assessment_questions` pgTable('assessment_questions'): id uuid pk defaultRandom; assessmentId uuid notNull references assessments.id onDelete cascade; questionId uuid notNull references questions.id onDelete cascade (Pitfall 4 — orphan prevention); sortOrder integer notNull default 0; marks integer notNull default 1. Table-level: unique('assessment_questions_unique').on(assessmentId, questionId) preventing duplicate question links. Import `questions` from './questions.js' and `users` from './users.js', `institutes` from './institutes.js'.
    2. Append `export { assessments, assessmentQuestions } from './examinations.js';` to packages/database/src/schema/index.ts (after the questions export line). Append the same two names to the re-export list in packages/database/src/index.ts (after the questions line).
    3. Run `pnpm db:generate` (root script → drizzle-kit generate in packages/database). Validate a packages/database/drizzle/0008_*.sql file appeared and the journal advanced (idx 7 = 0007 exists; next is 0008). Apply via the compose migrate service (`docker compose -f infrastructure/compose/docker-compose.yml run --rm migrate`) — do NOT run drizzle-kit push. The migration must contain CREATE TABLE "assessments", CREATE TABLE "assessment_questions", both FKs (assessment_questions.assessment_id → assessments cascade, question_id → questions cascade), the unique constraint, and the institutes/users FKs. Verify the emitted SQL (0007_brave_iron_monger.sql is the shape reference), never hand-edit it.
    4. Append an Assessment contracts section to packages/contracts/src/index.ts (mirror the Question section at :484-610): AssessmentStatusEnum (z.enum(['DRAFT','PUBLISHED','ACTIVE','COMPLETED'])); CreateAssessmentRequestSchema = { title: z.string().min(1).max(255), description: z.string().max(5000).optional(), durationMinutes: z.number().int().min(1).max(600).optional(), maxMarks: z.number().int().min(1).max(10000).optional(), instructions: z.record(z.string(), z.unknown()).optional(), startsAt: z.string().datetime().optional(), endsAt: z.string().datetime().optional() } .refine: if both startsAt and endsAt provided, new Date(startsAt) < new Date(endsAt) (path ['schedule'], message 'Schedule start must be before end'); UpdateAssessmentRequestSchema = same fields all optional with startsAt/endsAt additionally .nullable(); AssessmentResponseSchema = { id uuid, instituteId uuid, title, description .nullable(), durationMinutes .nullable(), maxMarks .nullable(), instructions .nullable(), startsAt datetime .nullable(), endsAt datetime .nullable(), status AssessmentStatusEnum, createdBy uuid, updatedBy uuid .nullable(), createdAt datetime, updatedAt datetime }; AssessmentListItemSchema = AssessmentResponseSchema.extend({ questionCount: z.number() }); Export all zod types (z.infer) per repo convention.
    5. Create apps/api/src/examinations/dto/create-assessment.dto.ts: class-validator DTO for create — title @IsString @MaxLength(255) required; description @IsOptional @IsString @MaxLength(5000); durationMinutes @IsOptional @IsInt @Min(1) @Max(600); maxMarks @IsOptional @IsInt @Min(1) @Max(10000); instructions @IsOptional @IsObject; startsAt/endsAt @IsOptional @IsISO8601 (class-validator, note the class-transformer @Type(() => Date) if the controller/service consumes Date objects — keep them as strings and cast in the service for the timestamp insert, matching how content uses datetimes). CRITICAL: NO status field, NO instituteId field — the global ValidationPipe (whitelist + forbidNonWhitelisted) turns any attempt to send them into a 400 (mass-assignment hardening). Create dto/assessment-query.dto.ts now with an empty class or status-filter placeholder comment so the file exists; actual filters (status) land in 08-02/08-03.
    6. Create apps/api/src/examinations/examinations.module.ts: clone questions.module.ts shape for ExaminationsController + ExaminationsService (no imports array — DatabaseModule is @Global()).
    7. Create apps/api/src/examinations/examinations.controller.ts: const WRITE_ROLES = ['INSTITUTE_ADMIN', 'TEACHER'] as const; @Controller('assessments'); @UseGuards(AccessTokenGuard, TenantGuard, RolesGuard) (imports from ../common/guards and ../common/decorators exactly as questions.controller.ts:17-31). Endpoints this task: POST / @HttpCode(HttpStatus.CREATED) @RequiredRoles(...WRITE_ROLES) create(@Tenant(), @CurrentUser(), @Body() CreateAssessmentDto) → { assessment }; GET /:assessmentId with ParseUUIDPipe → { assessment }; GET / list(@Tenant()) → { assessments }. Assessment param name: assessmentId everywhere.
    8. Create apps/api/src/examinations/examinations.service.ts (clone questions.service.ts anatomy): constructor @Inject(DATABASE_TOKEN) db; CreateAssessmentInput interface (title, description?, durationMinutes?, maxMarks?, instructions?, startsAt?, endsAt?); createAssessment(instituteId, createdBy, input) — server-side schedule validation (if both startsAt/endsAt provided and new Date(startsAt) >= new Date(endsAt) → BadRequestException('Schedule start must be before end')) then insert with values: instituteId, title, description: input.description ?? null, durationMinutes: input.durationMinutes ?? null, maxMarks: input.maxMarks ?? null, instructions: input.instructions ?? null, startsAt: input.startsAt ? new Date(input.startsAt) : null, endsAt: input.endsAt ? new Date(input.endsAt) : null, status: 'DRAFT' (server-computed — never read from the DTO), createdBy, updatedBy: createdBy, .returning(). getAssessment(instituteId, assessmentId) — where(and(eq(assessments.id, assessmentId), eq(assessments.instituteId, instituteId))).limit(1) + NotFoundException('Assessment not found') (questions.service.ts:108-121 shape). listAssessments(instituteId) — select assessments where eq(instituteId) orderBy desc(updatedAt), then LEFT JOIN count of assessment_questions per assessment to compute questionCount in application code (two queries: select all assessments, then select assessmentId + count from assessment_questions where assessmentId in the ids, group by assessmentId) OR a single select with leftJoin + sql count — choose the simplest Drizzle approach that returns questionCount on each row; do NOT add a questionCount column to the table (assumption A6 — computed at read time). No 409 mapping needed (no unique constraints on assessments).
    9. Register ExaminationsModule in apps/api/src/app/app.module.ts: add the import line and `ExaminationsModule,` to imports after QuestionsModule.
    10. Commit checkpoint per AGENTS.md Rule 8 after verification passes (feat(examinations): ...).
  </action>
  <acceptance_criteria>
    - packages/database/src/schema/examinations.ts exists with both tables; status and all enums are varchar, NOT pgEnum; assessment_questions has the unique(assessmentId, questionId) constraint and both FKs with onDelete cascade.
    - packages/database/drizzle/0008_*.sql exists (generated, not hand-written), the journal meta/_journal.json lists its tag, and the SQL contains CREATE TABLE "assessments", CREATE TABLE "assessment_questions", the unique constraint, and the FK constraints.
    - Migration applied cleanly: information_schema/psql shows both tables and the unique constraint on catlium_postgres/catlium_dev.
    - pnpm typecheck && pnpm lint pass across the workspace.
    - POST /api/v1/assessments with a valid body → 201, response assessment.status === "DRAFT", title echoed, questionCount not present on the single-object create response (it is a list-item concern).
    - GET /api/v1/assessments/:assessmentId → 200; GET /api/v1/assessments → array with questionCount on each row; GET random uuid → 404; institute-B-scoped GET of institute-A assessment id → 404 (anti-IDOR).
    - POST with instituteId or status in the body → 400 (forbidNonWhitelisted).
    - POST with startsAt in the past or endsAt before startsAt → 400.
  </acceptance_criteria>
  <done>A TEACHER can create a DRAFT assessment carrying duration/maxMarks/instructions/schedule, retrieve it by id, and list it with computed questionCount; the assessments + assessment_questions tables and migration 0008 are live; status/instituteId are rejected from bodies; schedule validation 400s hold.</done>
  <verify>
    <automated>
      # 1. Static gates
      pnpm typecheck && pnpm lint
      # 2. Migration generate + apply + DB validation (BLOCKING — no drizzle-kit push)
      pnpm db:generate
      ls packages/database/drizzle/0008_*.sql
      grep -c 'assessment_questions_unique' packages/database/drizzle/0008_*.sql
      docker compose -f infrastructure/compose/docker-compose.yml run --rm migrate
      docker compose exec postgres psql -U catlium -d catlium_dev -tAc "SELECT table_name FROM information_schema.tables WHERE table_name IN ('assessments','assessment_questions') ORDER BY table_name"
      docker compose exec postgres psql -U catlium -d catlium_dev -tAc "SELECT constraint_name FROM information_schema.table_constraints WHERE constraint_name='assessment_questions_unique'"
      # 3. E2E against the live stack (API_PORT=3000; reuse Phase 6/7 fixtures — teacher cookie + x-institute-id 11111111-...)
      #    a) POST /assessments {title:"Mock Exam",durationMinutes:60,maxMarks:100,instructions:{text:"Read carefully"},startsAt:<future>,endsAt:<later>} → expect 201 + status DRAFT
      #    b) GET /assessments/:id → 200; GET /assessments → array with questionCount 0; GET random uuid → 404
      #    c) anti-IDOR: institute-B member GET institute-A assessment id → 404
      #    d) mass-assignment: POST with "instituteId" → 400; with "status":"PUBLISHED" → 400
      #    e) schedule: POST startsAt past → 400; endsAt before startsAt → 400
      curl -s -b /tmp/opencode/q8.cookies -o /dev/null -w '%{http_code}' \
        -X POST http://localhost:3000/api/v1/assessments \
        -H 'Content-Type: application/json' -H 'x-institute-id: 11111111-1111-1111-1111-111111111111' \
        -d '{"title":"Mock Exam","durationMinutes":60,"maxMarks":100,"instructions":{"text":"Read carefully"},"startsAt":"2030-01-01T09:00:00.000Z","endsAt":"2030-01-01T11:00:00.000Z"}'
    </automated>
    <fails_when>
      - pnpm db:generate exits non-zero or produces no 0008_*.sql file.
      - The generated 0008 SQL lacks CREATE TABLE for either table, the unique constraint, or the cascade FKs.
      - The migrate command errors, or either psql query returns zero rows (tables/constraint not applied).
      - Any curl response other than: a) 201 + status DRAFT; b) 200/200/404; c) 404; d) 400/400; e) 400/400.
      - pnpm typecheck or pnpm lint reports errors in the new files.
    </fails_when>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Write the assessments API contract doc (docs/api/assessments.md)</name>
  <files>docs/api/assessments.md (new)</files>
  <read_first>
    docs/api/questions.md (full anatomy: header/auth note/error format, entity JSON sketch, endpoint sections, negative notes — literal template)
    08-RESEARCH.md "Code Examples" (:305-438) and State of the Art (:585-598) for the locked surface
  </read_first>
  <action>
    Write docs/api/assessments.md mirroring docs/api/questions.md's anatomy, documenting the FULL RESEARCH-locked surface for the module including the endpoints implemented in 08-02 (update, delete, add/remove questions) and 08-03 (publish, complete) — this doc is the contract those tasks implement against (repo: docs/api/ is the source of truth). Sections required:
    - Header + base URL `/api/v1/assessments` + auth note (cookie access_token + x-institute-id + membership 'active'; roles INSTITUTE_ADMIN/TEACHER for all writes) + error format (400/401/403/404 per global-exception.filter).
    - Entity JSON sketch of an Assessment: field names EXACTLY as CreateAssessmentDto/AssessmentResponseSchema name them (id, instituteId, title, description, durationMinutes, maxMarks, instructions, startsAt, endsAt, status, createdBy, updatedBy, createdAt, updatedAt) — name fields from the contract, NOT from memory.
    - Explicit lifecycle sentence: status transitions DRAFT → PUBLISHED → ACTIVE → COMPLETED with PUBLISHED → DRAFT unpublish allowed (EXAM-06/07); ACTIVE → DRAFT NOT allowed. Note the schedule is checked on reads (no cron), Phase 9 concern.
    - Endpoint sections: POST /api/v1/assessments (201; default status DRAFT; schedule validation — startsAt must be future/normal, endsAt after startsAt; status/instituteId never accepted → 400), GET /api/v1/assessments (list, questionCount computed, ordered updatedAt desc), GET /api/v1/assessments/:assessmentId (404 cross-institute), PATCH /:assessmentId (08-02; DRAFT-only edits, whitelist), DELETE /:assessmentId (08-02; 204, cross-institute 404), GET /:assessmentId/questions (08-02), POST /:assessmentId/questions (08-02; add questionIds, institute-scoped validation), DELETE /:assessmentId/questions/:questionId (08-02; 204), POST /:assessmentId/publish (08-03; publish validation gate: non-empty, all APPROVED, duration+maxMarks set, schedule valid), POST /:assessmentId/complete (08-03).
    - Explicit negative section: instituteId and status are never accepted in any request body → 400; only APPROVED questions may appear in a PUBLISHED assessment (EXAM-08).
  </action>
  <acceptance_criteria>
    - docs/api/assessments.md exists and contains a section for each of the module's endpoints with the exact paths and role gates above.
    - It states the lifecycle DRAFT → PUBLISHED → ACTIVE → COMPLETED and the PUBLISHED → DRAFT unpublish allowance.
    - It states the publish validation gate (all APP-row questions + duration + maxMarks + non-empty) covering EXAM-08.
    - It states that instituteId and status are rejected from request bodies.
    - It uses the exact field names from the contracts.
  </acceptance_criteria>
  <done>docs/api/assessments.md exists and fully documents the module surface, including the DRAFT→PUBLISHED→ACTIVE→COMPLETED lifecycle, the PUBLISHED→DRAFT unpublish allowance, and the approved-only publish gate (EXAM-08).</done>
  <verify>
    <automated>
      grep -q 'POST /api/v1/assessments' docs/api/assessments.md
      grep -q 'GET /api/v1/assessments' docs/api/assessments.md
      grep -q 'PUBLISHED' docs/api/assessments.md
      grep -q 'COMPLETED' docs/api/assessments.md
      grep -q 'DRAFT' docs/api/assessments.md
      grep -qE 'APPROVED' docs/api/assessments.md
      grep -q 'durationMinutes' docs/api/assessments.md
      grep -q 'maxMarks' docs/api/assessments.md
    </automated>
    <fails_when>
      - Any grep above returns nothing (missing lifecycle/state/fields/key-word → doc incomplete).
      - The doc lists instituteId or status inside any request body example (would contradict the mass-assignment hardening the code enforces).
    </fails_when>
  </verify>
</task>

</tasks>

### Artifacts this phase produces (Plan 08-01 scope; full phase covers 08-02/08-03/08-04)

- `assessments` Drizzle table (id, instituteId, title, description, durationMinutes, maxMarks, instructions, startsAt, endsAt, status default 'DRAFT', createdBy, updatedBy, createdAt, updatedAt) and `assessment_questions` join table (assessmentId, questionId, sortOrder, marks, unique(assessmentId, questionId)); barrel exports in `packages/database/src/schema/index.ts` and `packages/database/src/index.ts`
- Migration `packages/database/drizzle/0008_*.sql` + journal entry
- Contracts in `packages/contracts/src/index.ts`: `AssessmentStatusEnum`, `CreateAssessmentRequestSchema`, `UpdateAssessmentRequestSchema`, `AssessmentResponseSchema`, `AssessmentListItemSchema` (+ exported types)
- `apps/api/src/examinations/`: `ExaminationsModule`, `ExaminationsController` (POST /api/v1/assessments, GET /api/v1/assessments, GET /api/v1/assessments/:assessmentId), `ExaminationsService` (createAssessment, getAssessment, listAssessments), `CreateAssessmentDto`, `AssessmentQueryDto`
- `apps/api/src/app/app.module.ts` — ExaminationsModule registered
- `docs/api/assessments.md` — full module contract doc

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API (CREATE/READ) | unauthenticated/unscoped or role-deficient HTTP traffic crosses here; x-institute-id header is untrusted until TenantGuard validates membership |
| API→PostgreSQL | DTO-derived values become SQL insert values here; status and instituteId must be server-computed by this point |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Information Disclosure | GET /assessments/:assessmentId (`getAssessment`) | high | mitigate | Tenant-scoped `where(and(eq(assessments.id, assessmentId), eq(assessments.instituteId, instituteId)))` + NotFoundException on miss (questions.service.ts:108-121 pattern); E2E asserts institute-B-scoped GET of institute-A id → 404 |
| T-08-02 | Tampering | POST /assessments body (`CreateAssessmentDto`) | high | mitigate | DTO has NO status/instituteId fields; global ValidationPipe forbidNonWhitelisted turns smuggled fields into 400; status server-computed to 'DRAFT', instituteId injected from @Tenant() only |
| T-08-03 | Elevation of Privilege | POST /assessments (student creates an assessment) | high | mitigate | @RequiredRoles(...WRITE_ROLES) = INSTITUTE_ADMIN/TEACHER on the create handler + RolesGuard + TenantGuard active-membership check (lowercase 'active') |
| T-08-04 | Tampering | assessment_questions orphan rows on question delete | medium | mitigate | questionId FK carries onDelete cascade (Pitfall 4), matching questions.topicId cascade; verified in the generated 0008 SQL |
| T-08-05 | Tampering | schedule fields at create | low | mitigate | Zod CreateAssessmentRequestSchema.refine (endsAt after startsAt) + service-side BadRequestException; startsAt must be a datetime — prevents an immediately-expired or never-active assessment (Pitfall 6) |
| T-08-06 | Information Disclosure | GET /assessments list | medium | mitigate | listAssessments always ANDs eq(assessments.instituteId, instituteId); list is scoped by default |
| T-08-SC | Tampering | npm/pip/cargo installs | low | accept | ZERO new packages this phase (08-RESEARCH.md Package Legitimacy Audit — "no new external packages"); no install command appears in any task |
</threat_model>

<verification>
- `pnpm typecheck && pnpm lint` green.
- Migration 0008 exists, contains both CREATE TABLEs, the unique constraint, and cascade FKs; applies cleanly to catlium_postgres/catlium_dev (psql proof).
- E2E curl flow from Task 1 verify passes: 201/status DRAFT, 200/200/404 reads, cross-institute 404, mass-assignment 400s, schedule 400s.
- Contract doc grep gates pass.
</verification>

<success_criteria>
- A teacher can create an assessment (DRAFT by default) with duration, max marks, instructions, and a schedule window, then immediately retrieve it and list it with a computed questionCount.
- The assessments + assessment_questions tables are live in PostgreSQL with the unique-link invariant and cascade behavior enforced at the DB level.
- No request can set status or instituteId; no query can cross institutes.
- docs/api/assessments.md states the full contract the rest of the phase implements.
</success_criteria>

<output>
Create `.planning/phases/08-quiz-examination-management/08-01-SUMMARY.md` when done
</output>
