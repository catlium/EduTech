---
phase: 08-quiz-examination-management
plan: 02
title: Assessment CRUD completion — update (state-guarded), delete, and question add/remove via the join table
status: planned
date: 2026-09-04
type: execute
wave: 2
autonomous: true
depends_on: ["08-01"]
files_modified:
  - apps/api/src/examinations/examinations.controller.ts
  - apps/api/src/examinations/examinations.service.ts
  - apps/api/src/examinations/dto/update-assessment.dto.ts (new)
  - apps/api/src/examinations/dto/add-questions.dto.ts (new)
  - packages/contracts/src/index.ts
requirements: [EXAM-01, EXAM-02, EXAM-03, EXAM-04]
requirements_addressed: [EXAM-01, EXAM-02, EXAM-03, EXAM-04]

estimate:
  tokens: 52000
  raw_tokens: 26000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - PATCH /api/v1/assessments/:assessmentId updates title/description/durationMinutes/maxMarks/instructions/startsAt/endsAt only while status is DRAFT; updates to a PUBLISHED/ACTIVE/COMPLETED assessment return 400 (Pitfall 2 — state-guarded edits). Whitelist blocks status/instituteId → 400.
    - DELETE /api/v1/assessments/:assessmentId hard-deletes institute-scoped and returns 204; the join rows cascade; foreign-institute id → 404.
    - POST /api/v1/assessments/:assessmentId/questions adds questionIds with a per-row default sortOrder and marks, and rejects any question not in the active institute (Pitfall 3 — cross-tenant question linking blocked); duplicate link → 409/400.
    - DELETE /api/v1/assessments/:assessmentId/questions/:questionId removes the link and returns 204; foreign-institute → 404.
    - GET /api/v1/assessments/:assessmentId/questions returns the linked questions ordered by sortOrder, each with its marks and nested question data.
    - STUDENT members get 403 on every mutation/action; reads remain 200.
  artifacts:
    - examinations.controller.ts: PATCH, DELETE, POST/DELETE questions handlers + listQuestions
    - examinations.service.ts: updateAssessment, deleteAssessment, addQuestions, listQuestions, removeQuestion
    - dto/update-assessment.dto.ts, dto/add-questions.dto.ts
    - packages/contracts/src/index.ts: AddQuestionsRequestSchema, AssessmentQuestionSchema
  key_links:
    - updateAssessment must reject non-DRAFT status via the state guard (Pitfall 2) — this is the first edit-time state check.
    - addQuestions must JOIN questions on both id AND instituteId (Pitfall 3) — without it a teacher links another institute's question.
    - The join insert must wrap in a transaction + unique-violation handling so a duplicate (assessmentId, questionId) becomes a 409, not a crash.
    - marks defaults to 1 per row (research A5); sortOrder defaults to the next array index.
---

<objective>
Complete the assessment CRUD surface: PATCH update (state-guarded to DRAFT
only, re-validating schedule), the platform's second DELETE, and the
question-linking endpoints on the `assessment_questions` join table — add
institute-scoped questions (with per-row marks + sortOrder) and remove them,
plus a list endpoint returning the linked questions ordered by sortOrder.
This closes EXAM-01 (update/delete), EXAM-02 (add/remove questions), and the
update paths of EXAM-03 (duration/max marks/instructions) and EXAM-04
(scheduling).

Purpose: Expand the proven 08-01 tracer slice into the full requirement surface
without introducing new architectural patterns — every addition clones an
existing questions/content/academic counterpart.
Output: Complete, secured assessment CRUD + question linking, verified E2E
against the live stack.
</objective>

<execution_context>
@/home/ashif/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/ashif/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-quiz-examination-management/08-RESEARCH.md
Prior plan: `.planning/phases/08-quiz-examination-management/08-01-SUMMARY.md` (schema/contracts/service/controller built there — this plan extends those files; if the SUMMARY is missing, read 08-01-PLAN.md Task 1 for the symbols this plan extends)
</context>

### Multi-Source Coverage Audit (Plan 08-02)

| Source item | Where covered |
|-------------|---------------|
| GOAL: add/remove questions; duration/max marks/instructions; scheduling | T1/T2 (question linking), T1 (update path), T2 (update path) |
| REQ EXAM-01 update + delete | T1 (PATCH state-guarded), T1 (DELETE 204) |
| REQ EXAM-02 add/remove questions | T2 (POST/DELETE questions + list) |
| REQ EXAM-03 configure duration/max marks/instructions (update path) | T1 (PATCH re-validates these fields) |
| REQ EXAM-04 scheduling (update path) | T1 (PATCH re-validates startsAt/endsAt) |
| RESEARCH: join table with marks + sortOrder, not embedding in assessment row | T2 (per-row marks + sortOrder) |
| RESEARCH: Pitfall 3 cross-tenant question linking (JOIN on instituteId) | T2 (institute-scoped validation) |
| RESEARCH: Pitfall 2 state-guarded updates (DRAFT only) | T1 (update rejects non-DRAFT) |
| RESEARCH: Pitfall 4 join cascade (question delete → link cascade) | T1/T2 (schema cascade from 08-01 + remove endpoint) |
| RESEARCH: assumption A5 marks on join defaults 1; A6 questionCount computed | T2 |
| REQ EXAM-05 publish, EXAM-06 lifecycle, EXAM-07 transitions, EXAM-08 approved-only | 08-03 (state machine) |
| CONTEXT: none (no discuss-phase) | n/a |

Exclusions: student attempts (Phase 9), evaluation (Phase 10), analytics (Phase 12), frontend — out of scope per REQUIREMENTS.md/ROADMAP.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PATCH update (state-guarded to DRAFT) + DELETE (204) with schedule re-validation</name>
  <files>
    apps/api/src/examinations/dto/update-assessment.dto.ts (new)
    apps/api/src/examinations/examinations.controller.ts
    apps/api/src/examinations/examinations.service.ts
    packages/contracts/src/index.ts
  </files>
  <read_first>
    packages/contracts/src/index.ts Assessment contracts section (UpdateAssessmentRequestSchema from 08-01 — align the DTO with it)
    apps/api/src/questions/questions.service.ts:124-161 (updateQuestion + deleteQuestion pattern — literal template)
    apps/api/src/questions/questions.controller.ts:92-117 (PATCH/DELETE handler shape)
    08-RESEARCH.md Pitfall 2 (state-guarded edits: reject updates unless status is DRAFT; PUBLISHED limited) and Pitfall 6 (schedule re-validated on update)
  </read_first>
  <behavior>
    - Test 1: PATCH {title} on a DRAFT assessment → 200 with updated title; status still DRAFT.
    - Test 2: PATCH {durationMinutes, maxMarks, instructions} on the DRAFT assessment → 200, values replaced.
    - Test 3: PATCH setting startsAt after endsAt (both provided) → 400 (schedule re-validated).
    - Test 4: PATCH body containing status:"PUBLISHED" or instituteId → 400 (whitelist).
    - Test 5: PATCH on an assessment whose status is PUBLISHED (changed via the state machine in 08-03 OR directly via SQL for this test) → 400 (state-guarded edit).
    - Test 6: DELETE a DRAFT assessment → 204; subsequent GET → 404; DELETE random uuid → 404.
    - Test 7: institute-B-scoped PATCH/DELETE of an institute-A assessment id → 404 (anti-IDOR).
  </behavior>
  <action>
    Add UpdateAssessmentDto to apps/api/src/examinations/dto/update-assessment.dto.ts: all fields @IsOptional matching CreateAssessmentDto (title @IsString @MaxLength(255); description @IsString @MaxLength(5000); durationMinutes @IsInt @Min(1) @Max(600); maxMarks @IsInt @Min(1) @Max(10000); instructions @IsObject; startsAt/endsAt @IsOptional @IsISO8601, nullable via @ValidateIf((v) => v !== null) or a union). NO status, NO instituteId — PATCH can never change status/tenant (whitelist → 400). Note: status transitions are separate endpoints (08-03), never via PATCH.

    Ensure packages/contracts/src/index.ts UpdateAssessmentRequestSchema already mirrors this (created in 08-01) — if the startsAt/endsAt nullable form wasn't emitted there, correct it here so contract and DTO agree.

    In ExaminationsService:
    - updateAssessment(instituteId, userId, assessmentId, patch): first getAssessment(instituteId, assessmentId) (exists + tenant-owned); if existing.status !== 'DRAFT' throw BadRequestException('Assessment can only be edited in DRAFT status') (Pitfall 2 — state-guarded edit; PUBLISHED is locked until unpublished via 08-03); re-validate schedule if either startsAt/endsAt present (if both provided and new Date(startsAt) >= new Date(endsAt) → BadRequestException('Schedule start must be before end')); then update(assessments).set({ ...patch + date casts, updatedBy: userId, updatedAt: new Date() }).where(and(eq id, eq instituteId)).returning(); NotFoundException if no row (questions.service.ts:124-161 shape).
    - deleteAssessment(instituteId, assessmentId): delete(assessments).where(and(eq id, eq instituteId)).returning(); if no row → NotFoundException. Join rows cascade (onDelete cascade from 08-01 — no manual join cleanup needed).

    In ExaminationsController:
    - @Patch(':assessmentId') @RequiredRoles(...WRITE_ROLES): update(@Tenant(), @CurrentUser(), @Param('assessmentId', ParseUUIDPipe), @Body() UpdateAssessmentDto) → { assessment }.
    - @Delete(':assessmentId') @RequiredRoles(...WRITE_ROLES) @HttpCode(HttpStatus.NO_CONTENT): delete(@Tenant(), @Param('assessmentId', ParseUUIDPipe)) → await service.deleteAssessment(...); return nothing (204, no body). Import Delete from @nestjs/common (second @Delete in the platform after questions).
  </action>
  <acceptance_criteria>
    - PATCH on a DRAFT assessment updates title/durationMinutes/maxMarks/instructions and returns 200; status unchanged.
    - PATCH re-validates schedule (endsAt before startsAt → 400).
    - PATCH on a non-DRAFT assessment → 400 (state-guarded); PATCH with status/instituteId in body → 400.
    - DELETE a DRAFT assessment → 204 with empty body; GET after → 404; random uuid PATCH/DELETE → 404.
    - institute-B-scoped PATCH/DELETE of institute-A assessment → 404.
    - Contracts UpdateAssessmentRequestSchema and DTO agree (no field drift).
  </acceptance_criteria>
  <done>PATCH updates DRAFT assessments (title/config/schedule) with schedule re-validation and rejects non-DRAFT edits plus status/instituteId bodies; DELETE returns 204 and cascades; cross-institute and random-uuid calls return 404.</done>
  <verify>
    <automated>
      pnpm typecheck && pnpm lint
      # E2E (continues 08-01 fixtures)
      # PATCH happy path on DRAFT: expect 200, updated title, status DRAFT
      curl -s -b /tmp/opencode/q8.cookies -X PATCH http://localhost:3000/api/v1/assessments/<id> \
        -H 'Content-Type: application/json' -H 'x-institute-id: 11111111-1111-1111-1111-111111111111' \
        -d '{"durationMinutes":45,"maxMarks":50,"instructions":{"text":"Updated"}}'
      # PATCH bad schedule (endsAt before startsAt): expect 400
      # PATCH with status/instituteId in body: expect 400
      # PATCH non-DRAFT (SQL-update status to PUBLISHED first): expect 400
      # DELETE DRAFT: expect 204; GET after → 404; random-uuid DElETE → 404
      # anti-IDOR: institute-B PATCH/DELETE of institute-A assessment → 404
    </automated>
    <fails_when>
      - PATCH returns 200 for a non-DRAFT assessment, or for a body containing status/instituteId, or with an invalid schedule.
      - DELETE returns anything other than 204 for an in-institute DRAFT, or 200/204 for an institute-B-scoped id (tenant leak).
      - pnpm typecheck/lint fail.
    </fails_when>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Question linking — POST/DELETE questions, listQuestions on the join table, institute-scoped validation</name>
  <files>
    apps/api/src/examinations/dto/add-questions.dto.ts (new)
    apps/api/src/examinations/examinations.controller.ts
    apps/api/src/examinations/examinations.service.ts
    packages/contracts/src/index.ts
  </files>
  <read_first>
    apps/api/src/questions/questions.service.ts:124-161 (delete pattern) and :166-198 (approval/status update — the institute-scoped where shape)
    apps/api/src/questions/questions.controller.ts:183-229 (action handlers — literal template for the questions sub-resource)
    packages/contracts/src/index.ts Assessment contracts (AssessmentQuestionSchema from 08-01 draft)
    08-RESEARCH.md Pitfall 3 (JOIN on instituteId — cross-tenant linking) and A5 (marks default 1)
  </read_first>
  <behavior>
    - Test 1: POST /assessments/:id/questions with a valid APPROVED in-institute questionId → 201, link row returned with sortOrder and marks 1.
    - Test 2: POST with a questionId from another institute → 400/404 (Pitfall 3).
    - Test 3: POST with a questionId already linked to the assessment → 409 (unique constraint) or 400.
    - Test 4: GET /assessments/:id/questions → array of linked questions ordered by sortOrder, each nesting its question data + marks.
    - Test 5: DELETE /assessments/:id/questions/:questionId → 204; subsequent GET no longer lists it; GET random questionId → 404.
    - Test 6: GET listQuestions for an assessment with no links → empty array.
  </behavior>
  <action>
    Ensure AddQuestionsRequestSchema is in packages/contracts/src/index.ts (created in 08-01 draft as { questionIds: z.array(z.string().uuid()).min(1) }) and AssessmentQuestionSchema (id, assessmentId, questionId, sortOrder, marks + nested QuestionResponseSchema as `question`); add here if not yet present.

    Create dto/add-questions.dto.ts: AddQuestionsDto = { questionIds: @IsUUID(undefined, { each: true }) @ArrayMinSize(1) questionIds } (batch-question-action.dto.ts:25-28 template).

    In ExaminationsService add:
    - listQuestions(instituteId, assessmentId): first getAssessment(instituteId, assessmentId) (tenant-owned); then select assessment_questions joined to questions WHERE eq(assessmentId) AND eq(questions.instituteId, instituteId) ORDER BY sortOrder asc; return rows each mapped to { id, assessmentId, questionId, sortOrder, marks, question: <the nested question row> }.
    - addQuestions(instituteId, assessmentId, questionIds: string[]): getAssessment first (tenant-owned + NotFound); for each id run a SELECT from questions WHERE eq(questions.id, id) AND eq(questions.instituteId, instituteId).limit(1) — if any is missing, throw BadRequestException(`Question ${id} not found or not in this institute`) (Pitfall 3 — the JOIN on instituteId blocks cross-tenant linking). Then in a db.transaction, insert rows into assessment_questions with assessmentId, questionId, sortOrder = 1-based array index, marks = 1 (default, research A5), each .returning(); wrap the insert in a try/catch — on a unique-violation (assessmentId, questionId) rethrow as ConflictException/BadRequestException('Question already in this assessment'). Return the added rows.
    - removeQuestion(instituteId, assessmentId, questionId): getAssessment first; delete(assessment_questions).where(and(eq assessmentId, eq questionId)).returning(); if no row → NotFoundException('Assessment question not found'). The questionId institute-check is covered by assessment ownership (the join only links in-institute assessments); still safe.

    In ExaminationsController add (all @RequiredRoles(...WRITE_ROLES) for mutations, reads open to members):
    - GET ':assessmentId/questions' → listQuestions → { questions }
    - POST ':assessmentId/questions' @RequiredRoles(...WRITE_ROLES) → addQuestions(tenant.instituteId, assessmentId, dto.questionIds) → { added } (the array of link rows)
    - DELETE ':assessmentId/questions/:questionId' @RequiredRoles(...WRITE_ROLES) @HttpCode(HttpStatus.NO_CONTENT) → removeQuestion → 204
  </action>
  <acceptance_criteria>
    - POST questions adds in-institute questions with sortOrder + marks 1; response 201/200 with the link rows.
    - Cross-institute questionId → 400 (Pitfall 3); duplicate link → 409/400.
    - GET :id/questions returns linked questions ordered by sortOrder with nested question data + marks; empty array when none.
    - DELETE :id/questions/:questionId → 204; listing after reflects the removal; random questionId → 404.
    - STUDENT POST/DELETE questions → 403 (swept in Task 3).
  </acceptance_criteria>
  <done>Questions can be added to and removed from an assessment via the join table with per-row marks/sortOrder; cross-institute and duplicate links are rejected; GET :id/questions lists sorted links with nested question data.</done>
  <verify>
    <automated>
      pnpm typecheck && pnpm lint
      # E2E: need 2+ APPROVED in-institute questions (create MANUAL MCQ + TRUE_FALSE via /questions — they auto-approve)
      # POST /assessments/:id/questions {questionIds:[q1,q2]} → 201, 2 link rows, sortOrder 1 and 2, marks 1
      # GET /assessments/:id/questions → array of 2 with nested question + marks
      # POST with an institute-B questionId → 400
      # POST with q1 again → 409/400 (duplicate)
      # DELETE /assessments/:id/questions/q1 → 204; GET list → 1 item
      # student cookie POST/DELETE questions → 403
    </automated>
    <fails_when>
      - A cross-institute questionId returns 2xx (tenant leak on linking — Pitfall 3).
      - A duplicate link returns 2xx or crashes instead of 409/400.
      - listQuestions is not sorted by sortOrder or returns another institute's questions.
      - Student POST/DELETE questions returns 2xx (privilege escalation).
      - pnpm typecheck/lint fail.
    </fails_when>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Student security sweep + questions sub-resource cross-institute 404 verification</name>
  <files>
    apps/api/src/examinations/examinations.controller.ts
    apps/api/src/examinations/examinations.service.ts
  </files>
  <read_first>
    08-RESEARCH.md Security Domain table (:668-688: 404-vs-403 semantics, tenant isolation)
    docs/user-validation.md Phase 6 security sweep (STUDENT 403s + institute-B 404s pattern to replicate)
  </read_first>
  <action>
    Run a security verification sweep and record outcomes in the task summary (mirror these from Phase 6): 
    1. Register a STUDENT user, bind a membership to institute A with role 'STUDENT' (status lowercase 'active' — SQL inserts via docker compose exec postgres psql).
    2. With the student cookie: POST /assessments → 403; PATCH → 403; DELETE → 403; POST/DELETE :id/questions → 403; GET list and GET :id and GET :id/questions → 200 (reads allowed, writes denied).
    3. Register a second teacher, bind to institute B (SQL: institutes insert + membership + INSTITUTE_ADMIN role). With B's header on institute-A assessment id: GET/PATCH/DELETE → 404; POST/DELETE :id/questions → 404 (anti-IDOR on the questions sub-resource too).
    4. Confirm the global exception filter shape ({ statusCode, message, error }) on every non-2xx.
    Fix any gap found (e.g., a missing @RequiredRoles on a mutation) in examinations.controller.ts before closing.
  </action>
  <acceptance_criteria>
    - Student POST/PATCH/DELETE/actions → 403; reads → 200.
    - Institute-B member on institute-A assessment (including questions sub-resource) → 404 every time.
    - All non-2xx bodies match { statusCode, message, error }.
    - pnpm typecheck && pnpm lint pass.
  </acceptance_criteria>
  <done>Security sweep passes: student mutations/actions → 403 with reads 200; institute-B member on institute-A assessment (including questions sub-resource) → 404 each; all errors use the global filter shape.</done>
  <verify>
    <automated>
      pnpm typecheck && pnpm lint
      # E2E (student + institute-B fixtures from the sweep; teacher cookie for happy paths)
      # student: POST /assessments → 403; PATCH → 403; DELETE → 403; POST /:id/questions → 403; GET list/get/:id/questions → 200
      # institute-B member on institute-A assessment id: GET/PATCH/DELETE/POST-questions → 404 each
      # verify exception filter shape on each non-2xx body
    </automated>
    <fails_when>
      - Any student mutation returns 2xx (privilege escalation — RolesGuard bypassed).
      - Any institute-B-scoped action on an institute-A assessment returns 2xx.
      - Error responses deviate from the global exception filter shape.
    </fails_when>
  </verify>
</task>

</tasks>

### Artifacts this phase produces (Plan 08-02 scope)

- `ExaminationsService` additions (`apps/api/src/examinations/examinations.service.ts`): `updateAssessment` (state-guarded), `deleteAssessment` (platform's second delete), `addQuestions`, `listQuestions`, `removeQuestion`
- `ExaminationsController` additions: `@Patch(':assessmentId')`, `@Delete(':assessmentId')` (204), `GET ':assessmentId/questions'`, `POST ':assessmentId/questions'`, `DELETE ':assessmentId/questions/:questionId'` (204)
- `UpdateAssessmentDto`, `AddQuestionsDto` (`apps/api/src/examinations/dto/`)
- Contracts (`packages/contracts/src/index.ts`): `AddQuestionsRequestSchema`, `AssessmentQuestionSchema` (if not already from 08-01), corrected `UpdateAssessmentRequestSchema`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API (UPDATE/DELETE/QUESTION-LINK) | mutation traffic crosses here; the questions sub-resource introduces a second write path where tenant scope must be enforced |
| API→PostgreSQL | PATCH set-values, DELETE predicates, and join-insert values become SQL here; PATCH and DELETE predicates MUST carry instituteId |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-07 | Tampering | DELETE /assessments/:assessmentId (`deleteAssessment`) | high | mitigate | Predicate `and(eq(assessments.id, assessmentId), eq(assessments.instituteId, instituteId))` + NotFoundException on miss; joins cascade via onDelete. E2E asserts institute-B delete of institute-A assessment → 404. |
| T-08-08 | Elevation of Privilege | all mutation handlers (PATCH/DELETE/add/remove questions) | high | mitigate | Every handler carries @RequiredRoles(...WRITE_ROLES); student sweep asserts 403 on all four/question sub-resource mutations, 200 on reads |
| T-08-09 | Tampering | PATCH /assessments/:assessmentId body | medium | mitigate | UpdateAssessmentDto whitelists title/description/durationMinutes/maxMarks/instructions/startsAt/endsAt only; UpdateAssessmentRequestSchema lacks status/instituteId → 400; DRAFT-only guard rejects edits to PUBLISHED/ACTIVE/COMPLETED (Pitfall 2) |
| T-08-10 | Tampering | POST /assessments/:id/questions (cross-tenant linking) | high | mitigate | addQuestions validates each questionId with `where(and(eq(questions.id, id), eq(questions.instituteId, instituteId)))` (Pitfall 3); a foreign-institute questionId → 400; duplicate link → 409 via unique-violation mapping |
| T-08-11 | Tampering | assessment_questions orphan rows / duplicate links | medium | mitigate | questionId FK onDelete cascade (08-01) prevents orphans (Pitfall 4); unique(assessmentId, questionId) + violation mapping prevents duplicate links |
| T-08-12 | Information Disclosure | GET :id/questions leaking another institute's questions | high | mitigate | listQuestions JOINs assessment_questions to questions with `eq(questions.instituteId, instituteId)`; any leaked row would surface a cross-institute question — E2E asserts the joined rows are all in-institute |
| T-08-13 | Tampering | schedule fields on update | low | mitigate | updateAssessment re-runs the endsAt-after-startsAt check when either value changes (Pitfall 6) |
| T-08-SC | Tampering | npm/pip/cargo installs | low | accept | No installs in this plan; dependency surface unchanged from 08-01 (audited in 08-RESEARCH.md) |
</threat_model>

<verification>
- `pnpm typecheck && pnpm lint` green.
- E2E: update/delete/question-linking behave per acceptance criteria; security sweep (student 403s, institute-B 404s incl. questions sub-resource, error-shape) recorded in the Task 3 summary.
- No 2xx cross-institute result; no student mutation succeeds.
</verification>

<success_criteria>
- Assessment CRUD is complete: create (08-01), read/list, update (DRAFT-guarded), delete — and question linking works with per-row marks/sortOrder, blocking cross-institute and duplicate links.
- A student cannot mutate any assessment or its questions; a member of another institute cannot touch this institute's assessments or link foreign questions by guessing ids.
</success_criteria>

<output>
Create `.planning/phases/08-quiz-examination-management/08-02-SUMMARY.md` when done
</output>
