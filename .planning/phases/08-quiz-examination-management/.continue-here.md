---
context: milestone
milestone: demo-milestone
task: 0
total_tasks: 4
status: active
last_updated: 2026-09-08T00:00:00.000Z
---

# State — Demo Milestone (resume point)

## Where We Are

Phase 8 (quiz-examination-management) is **COMPLETE and closed**, including the
08-06 / 08-07 gap closures (WR-01..WR-05). E2E suite `scripts/e2e/p8_e2e.sh`
is at **PASS=86 FAIL=0**. Demo Wave 0 (seed + memberships) is complete. The
project has moved to a **demo-first vertical-slice strategy** (user-directed):
get a working end-to-end model with a real, polished UI as soon as possible.

## Current Focus

Demo Milestone vertical slice:

```text
Teacher/Admin → Login → Create Subject → Upload Syllabus → Generate/confirm
Chapters + Topics → Upload Material → Process/OCR → Generate AI Notes →
Create/Generate Questions → Create Quiz → Publish Quiz
→ Student Login → View Available Quiz → Start Attempt → Answer → Submit →
Automatic Evaluation → View Result
```

## Next Tasks (in order)

1. **Wave 1 — Syllabus backend** (migration 0009 `syllabus_proposals`, worker
   op `AI_GENERATE_SYLLABUS`, API module `apps/api/src/syllabus`:
   generate/get/patch/confirm, proposal-only AI with teacher confirmation).
2. **Wave 3 — Frontend `apps/web`** (Next.js 15 + shadcn/ui, teacher + student
   UI) — parallelizable with Wave 1-2 backend where APIs are stable.
3. **Wave 2 — Attempts backend** (migration 0010, API module `apps/api/src/attempts`,
   sanitized projection closes WR-06, deterministic grading).
4. **Wave 4 — Full integration & demo validation** (`demo_e2e.sh`, browser walkthrough).

## Critical Constraints (still apply)

- **Baked-API-image rebuild** — the catlium-api container runs dist from the
  baked Docker image (no source mount). Any service change requires
  `docker compose -f infrastructure/compose/docker-compose.yml build api && up -d api`
  before E2E.
- **Fixture cookies go stale** — re-issue teacher/institute/student cookies
  before every E2E run (see `docs/user-validation.md` fixtures block).
- **Keep `p8_e2e.sh` PASS=86 FAIL=0 green** through Waves 1–2.

## Sources of Truth

- `AGENTS.md` — project rules (checkpoints, docs/tasks.md, project-status.md).
- `docs/architecture/demo-milestone.md` — master execution plan (source of truth).
- `.planning/ROADMAP.md` — authoritative demo-first roadmap.
- `.planning/STATE.md` — updated state.
- `docs/tasks.md` — task tracker with Wave 1 next.
- `docs/project-status.md` — project status with Phase 8 closed.