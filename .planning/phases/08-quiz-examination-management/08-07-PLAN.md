---
phase: 08-quiz-examination-management
plan: 07
title: "Gap closure — sortOrder gaps + DELETE guard + phase docs closure (WR-04, WR-05, EXAM-02/05)"
status: planned
date: 2026-09-05
type: execute
wave: 3
autonomous: true
depends_on: ["08-06"]
gap_closure: true
files_modified:
  - apps/api/src/examinations/examinations.service.ts
  - packages/database/scripts/resync-assessment-sort-order.sql
  - docs/api/assessments.md
  - /tmp/opencode/p8_e2e.sh
  - docs/user-validation.md
  - docs/project-status.md
  - docs/tasks.md
requirements: [EXAM-02, EXAM-05]

estimate:
  tokens: 36000
  raw_tokens: 18000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Within an assessment, linked questions have UNIQUE, CONTIGUOUS-NOT-REQUIRED sortOrder values: appending questions computes `base = max(sortOrder)` ONCE per assessment inside the transaction and inserts `base + i + 1` — no mid-loop per-append queries, no duplicates, no gaps from lost max reads (VERIFICATION gap 4 — existing assessments already carry duplicate offsets)."
    - "The two known-duplicate assessments (ids from 08-VERIFICATION.md gap 4) are resynced by a tracked SQL script to a deterministic order `(sort_order, id)`; the global invariant query `GROUP BY assessment_id, sort_order HAVING count(*)>1` returns 0 rows; NO drizzle migration, NO schema change."
    - "deleteAssessment rejects any non-DRAFT assessment: DELETE on PUBLISHED, ACTIVE, or COMPLETED assessment → 400 'Only DRAFT assessments can be deleted; unpublish or complete first'; DELETE on a DRAFT assessment still → 200/204."
    - "docs/api/assessments.md Delete section documents the DRAFT-only rule; docs/user-validation.md EXAM-02/05 record the sortOrder and delete-guard checks with all markers green."
    - "docs/project-status.md and docs/tasks.md are updated to phase-closed state: 08-05..08-07 (gap closure) complete, suite green at PASS=70 FAIL=0."
  artifacts:
    - path: "apps/api/src/examinations/examinations.service.ts"
      provides: "addQuestions max-offset computation; deleteAssessment status guard"
    - path: "packages/database/scripts/resync-assessment-sort-order.sql"
      provides: "tracked, re-runnable resync for the 2 known duplicate assessments"
    - path: "docs/api/assessments.md"
      provides: "Delete section states DRAFT-only; add-questions ordering semantics documented"
    - path: "/tmp/opencode/p8_e2e.sh"
      provides: "5 new gap cases (2 sortOrder + 3 delete-guard) → suite PASS=70"
    - path: "docs/user-validation.md"
      provides: "EXAM-02/05 gap checks with green markers"
    - path: "docs/project-status.md, docs/tasks.md"
      provides: "phase closure state (AGENTS.md Rule 3/4 checkpoint)"
  key_links:
    - from: "addQuestions transaction"
      to: "questions.sortOrder uniqueness"
      via: "ONE `max(questions.sortOrder)` per assessment before the batch insert; `import { max } from 'drizzle-orm'` (no per-iteration queries)"
    - from: "deleteAssessment"
      to: "assessments.status"
      via: "status read inside the existing delete flow; only 'DRAFT' passes the guard"
    - from: "resync script + verification"
      to: "questions table state"
      via: "psql against DATABASE_URL (no tsx runner exists in apps/api — SQL-only resync, verified by the GROUP BY/HAVING query)"
---

<objective>
Close the last two failed verification truths:
- **WR-04 (gap 4):** `addQuestions` computes each appended question's sortOrder inside the loop with fresh queries, so a concurrent/past-timing sequence produced duplicate offsets in the two known assessments (and can again). The fix: compute `base = max(sortOrder)` once per assessment inside the transaction, insert at `base + i + 1`, and ship a tracked SQL resync script that repairs the existing duplicate rows to a deterministic order, with a verification query that must return 0 duplicate groups.
- **WR-05 (gap 5):** DELETE deletes any assessment regardless of state. The fix: a DRAFT-only guard (400 for PUBLISHED/ACTIVE/COMPLETED). **Rated `costly`** — recorded and flagged for human awareness, with the defer-to-Phase-9 alternative documented as rejected (see Assumptions).

Then close the phase docs (docs/project-status.md + docs/tasks.md per AGENTS.md Rules 3/4) and land the suite at PASS=70 FAIL=0 — the last E2E milestone for the 08 phase.

Purpose: sortOrder duplication silently corrupts the question-ordering contract for future papers (the ordering is re-read at paper assembly), and unconditional DELETE is a destructive write-path gap. Both are the phase's final open truths.
Output: sortOrder invariant enforced + existing data repaired + server-side delete guard + docs closed + suite PASS=70.
</objective>

<execution_context>
@/home/ashif/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/ashif/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-quiz-examination-management/08-VERIFICATION.md (gaps 4 + 5 — authoritative missing items INCLUDING the two assessment ids and the exact human-verification queries)
@.planning/phases/08-quiz-examination-management/08-REVIEW.md (WR-04 :96-105 max-offset finding, WR-05 :107-114 delete guard finding)
@.planning/phases/08-quiz-examination-management/08-RESEARCH.md (Pitfall 4 — batch inserts with identical computed offsets; write-path trust boundary)
Prior plans: `.planning/phases/08-quiz-examination-management/08-05-PLAN.md`, `08-06-PLAN.md` (harness now at PASS=65 FAIL=0; this plan's E2E cases build on the same fixture pool)
Docs: `docs/project-status.md`, `docs/tasks.md` (AGENTS.md Rules 3/4 checkpoint targets)
</context>

### Multi-Source Coverage Audit (Plan 08-07)

| Source item | Where covered |
|-------------|---------------|
| VERIFICATION gap 4 (failed truth): duplicate/missing sortOrder offsets in the two known assessments | T1 (resync script + psql run), T1 (verification query → 0 duplicate groups) |
| VERIFICATION gap 4 missing items: max-offset computation once per assessment; `base + i + 1` inserts; no mid-loop queries | T1 (exact addQuestions rewrite) |
| VERIFICATION gap 4 human-check: re-run the ordering assertions from the verification docs | T1 (verification query + E2E append/ordering cases) |
| VERIFICATION gap 5 (failed truth): DELETE removes non-DRAFT assessments | T2 (status guard), T2 (E2E PUBLISHED/ACTIVE/COMPLETED → 400) |
| VERIFICATION gap 5 missing item: DRAFT-only deletability | T2 (exact guard message + DRAFT regression) |
| REQ EXAM-05: linked questions keep a stable, deterministic order | T1 (sortOrder invariant + E2E ordering check) |
| REQ EXAM-02: assessment lifecycle/delete semantics safe | T2 (DRAFT-only guard) |
| REVIEW WR-04/WR-05 fix templates | T1/T2 (exact shapes) |
| RESEARCH Pitfall 4 | T1 (batch insert with computed offsets — one read, deterministic sequence) |
| AGENTS.md Rules 3/4 | T3 (project-status.md + tasks.md closure) |

Exclusions: WR-06 (answer-key exposure) — deferred to Phase 9; frontend delete-button gating — Phase 18 concern, out of scope for the API guard.

<tasks>

<task type="auto">
  <name>Task 1: addQuestions max-offset sortOrder + tracked SQL resync of the duplicate assessments (WR-04, EXAM-05)</name>
  <files>
    apps/api/src/examinations/examinations.service.ts
    packages/database/scripts/resync-assessment-sort-order.sql
    /tmp/opencode/p8_e2e.sh
    docs/user-validation.md
  </files>
  <read_first>
    .planning/phases/08-quiz-examination-management/08-VERIFICATION.md gap 4 (the TWO assessment ids + the exact verification queries the resync must satisfy)
    .planning/phases/08-quiz-examination-management/08-REVIEW.md WR-04 (:96-105)
    apps/api/src/examinations/examinations.service.ts:247-292 (addQuestions — the mid-loop sortOrder computation at :274-284)
    packages/database/src/schema/examinations.ts questions table (sortOrder column default 0, the assessmentId unique-index situation)
    packages/database/package.json (no tsx runner — resync must be psql-executed SQL, per session finding)
    .env (DATABASE_URL=postgresql://catlium:catlium_dev_secret@localhost:5432/catlium_dev)
  </read_first>
  <action>
    1. `addQuestions` sortOrder rewrite (:274-284): inside the existing transaction, BEFORE the loop, run ONE aggregate:
       `const [agg] = await db.select({ maxSort: max(questions.sortOrder) }).from(questions).where(eq(questions.assessmentId, assessmentId))`
       then assign `sortOrder: (agg?.maxSort ?? 0) + i + 1` in the loop (i = insertion index, 0-based). Add `max` to the existing `drizzle-orm` import. DELETE the old mid-loop per-append select. Behavior: appending to an assessment whose questions end at sortOrder 5 yields 6,7,... — no duplicate-offset possibility from stale reads. Deduplicate questionIds in the same request before computing max? NO — keep current behavior unless the verification gap explicitly lists it; this task changes ONLY the offset computation.
    2. Create `packages/database/scripts/resync-assessment-sort-order.sql` — git-tracked, re-runnable:
       - For the 2 assessment ids read from 08-VERIFICATION.md gap 4, renumber `questions.sort_order` deterministically per assessment: row_number() over (partition by assessment_id order by sort_order, id) starting at 1 (the first row keeps 1). Use a single UPDATE ... FROM (SELECT id, row_number() ...) form so the file is idempotent (re-running re-numbers identically) and safe (no schema DDL — the schema-gate forbids migrations here; this is DATA REPAIR ONLY).
       - Header comment: file purpose, source (08-VERIFICATION.md gap 4), safety note (idempotent, transaction-wrapped via BEGIN/COMMIT, no DDL), and the expected post-state (0 duplicate groups).
    3. RUN the resync via psql. Preferred: `psql "$DATABASE_URL" -f packages/database/scripts/resync-assessment-sort-order.sql` (psql must be on PATH). If not: `docker compose -f infrastructure/compose/docker-compose.yml exec -T postgres psql -U catlium -d catlium_dev -f packages/database/scripts/resync-assessment-sort-order.sql` (container/service name per infrastructure/compose/docker-compose.yml). Record the exact command + output in the task summary.
    4. VERIFY the invariant globally — this is the WR-04 human-check query:
       `SELECT assessment_id, sort_order, count(*) FROM questions WHERE assessment_id IN (<the 2 ids>) GROUP BY assessment_id, sort_order HAVING count(*) > 1;`
       → 0 rows. Also spot-check each of the 2 assessments is contiguous 1..n with no holes: `SELECT assessment_id, count(*) AS n, min(sort_order), max(sort_order), count(DISTINCT sort_order) FROM questions WHERE assessment_id IN (...) GROUP BY assessment_id;` → max = n and count(DISTINCT) = n.
    5. E2E — append 2 cases to the harness QUESTION-ORDERING block:
       (a) append a fresh 2-question batch to an existing DRAFT assessment A (already holding questions) → `ok "$ST" 200 "append to existing assessment -> 200"` (+1 PASS);
       (b) GET /assessments/:id (questions included) → the response lists the questions in stable order (grep the returned questionIds array order — new ids appear after the pre-existing ones) (+1 PASS).
       Baseline for this run: PASS=65 FAIL=0 from 08-06 — reproduce it first, then add. Final after this task: PASS=67 FAIL=0.
    6. docs/user-validation.md EXAM-05 section: record the sortOrder continuity rule (append continues at max+1; no duplicates) + the resync ids + the verification query; markers stay [x].
    Do NOT change the questions schema, do NOT run db:generate, do NOT touch question deletion/reordering beyond the offset computation.
  </action>
  <acceptance_criteria>
    - addQuestions reads max(sortOrder) exactly ONCE per assessment, inside the transaction, before the loop (grep: no select inside the loop; `max(` from drizzle-orm).
    - The resync SQL file exists, is tracked, idempotent, transaction-wrapped, DDL-free.
    - psql ran it; the GROUP BY/HAVING duplicate query returns 0 rows for the 2 ids; the contiguity check returns max = count = count(DISTINCT) per assessment.
    - Harness reproduces PASS=65 then lands PASS=67 FAIL=0 with the 2 append/ordering cases.
    - docs/user-validation.md EXAM-05 documents the rule + verification query, markers green.
  </acceptance_criteria>
  <done>The sortOrder invariant is enforced at write time (one max-read, base + i + 1), the two known-duplicate assessments are resynced to deterministic order with 0 duplicate groups proven by the verification query, and the harness proves appends + ordering at PASS=67.</done>
  <verify>
    <automated>
      # invariant query against live DB — must print 0 rows
      psql "$DATABASE_URL" -c "SELECT assessment_id, sort_order, count(*) FROM questions WHERE assessment_id IN (<the 2 ids from 08-VERIFICATION.md>) GROUP BY assessment_id, sort_order HAVING count(*) > 1;" 2>&1 | grep -c "^ *[0-9]"
      # must be 0
      # no mid-loop selects remain: the loop body contains only the push
      grep -c "sortOrder: (agg" apps/api/src/examinations/examinations.service.ts
      bash /tmp/opencode/p8_e2e.sh 2>&1 | tail -5   # MUST read: RESULT: PASS=67 FAIL=0
    </automated>
    <fails_when>
      - The duplicate-group query prints any row (resync incomplete or wrong ids).
      - The loop still performers a per-append sortOrder query (the max-read is missing or placed inside the loop).
      - The suite reports anything but PASS=67 FAIL=0.
    </fails_when>
    <human-check>
      Confirm the resync psql output + 0-row duplicate query in the task summary — this is VERIFICATION gap 4's human-check verbatim (data repair + ordering assertions).
    </human-check>
  </verify>
</task>

<task type="auto">
  <name>Task 2: DRAFT-only DELETE guard (WR-05, EXAM-02)</name>
  <files>
    apps/api/src/examinations/examinations.service.ts
    docs/api/assessments.md
    /tmp/opencode/p8_e2e.sh
    docs/user-validation.md
  </files>
  <read_first>
    .planning/phases/08-quiz-examination-management/08-VERIFICATION.md gap 5 (failed truth + missing items — DELETE removed non-DRAFT assessments at verification time)
    .planning/phases/08-quiz-examination-management/08-REVIEW.md WR-05 (:107-114)
    apps/api/src/examinations/examinations.service.ts:211-220 (deleteAssessment — current guard state, if any)
    packages/database/src/schema/examinations.ts assessments table (status column values: DRAFT/PUBLISHED/ACTIVE/COMPLETED + the draft junction)
    apps/api/src/examinations/examinations.controller.ts (DELETE route + guard decorators on the endpoint)
    docs/api/assessments.md:167-174 (Delete section)
  </read_first>
  <action>
    1. In `deleteAssessment`, BEFORE deletion: read the assessment's status (single select — do not add a second round-trip pattern; choose the existing fetch if the flow already loads the row). If status !== 'DRAFT' → `throw new BadRequestException('Only DRAFT assessments can be deleted; unpublish or complete first')`. Only DRAFT rows proceed to the existing delete statements. If the current implementation already loads the assessment row for scope/tenancy checks, add the status check on THAT row (do not duplicate the fetch). Keep the existing 404 semantics for missing assessments unchanged.
    2. docs/api/assessments.md Delete section (:167-174): document — only DRAFT assessments can be deleted; PUBLISHED/ACTIVE/COMPLETED return 400 ('Only DRAFT assessments can be deleted; unpublish or complete first'); unpublishing an assessment returns it to DRAFT before deletion is possible.
    3. E2E — add exactly 3 delete-guard cases to the harness lifecycle block (baseline PASS=67 FAIL=0 first):
       (a) DELETE a PUBLISHED assessment (fixture A_pub) → `ok "$ST" 400 "DELETE published assessment -> 400"` (+1 PASS);
       (b) DELETE an ACTIVE (running) assessment (fixture A_run, status ACTIVE) → `ok "$ST" 400 "DELETE active assessment -> 400"` (+1 PASS);
       (c) DELETE a COMPLETED assessment (fixture A_done, status COMPLETED) → `ok "$ST" 400 "DELETE completed assessment -> 400"` (+1 PASS);
       and regression: the EXISTING DRAFT-delete success case must still pass (do NOT add a new one — the count is pinned at 3).
       Final: PASS=70 FAIL=0.
    4. docs/user-validation.md EXAM-02 case: record the 3 guarded DELETE results + the DRAFT exception; markers stay [x].
    Note the severity framing: rejecting is one `if` + throw — do NOT build a delete-policy module, do NOT add soft-delete columns (schema-gate), do NOT touch the questions-deletion cascade behavior.
  </action>
  <acceptance_criteria>
    - DELETE on PUBLISHED/ACTIVE/COMPLETED → 400 with the exact message; DELETE on DRAFT → unchanged success.
    - The status check reuses the already-loaded row (no added round-trip); missing-assessment 404 unchanged.
    - docs/api/assessments.md Delete section states the DRAFT-only rule.
    - Harness reproduces PASS=67 then lands PASS=70 FAIL=0 with exactly 3 new delete-guard cases.
  </acceptance_criteria>
  <done>No assessment in PUBLISHED/ACTIVE/COMPLETED state can be deleted (400, exact message); DRAFT deletes still succeed; the rule is documented and E2E-covered.</done>
  <verify>
    <automated>
      pnpm typecheck && pnpm lint
      grep -n "Only DRAFT assessments can be deleted" apps/api/src/examinations/examinations.service.ts
      bash /tmp/opencode/p8_e2e.sh 2>&1 | tail -5   # MUST read: RESULT: PASS=70 FAIL=0
      bash /tmp/opencode/p8_e2e.sh 2>&1 | grep -c "DELETE active assessment -> 400"
      # must be 1
    </automated>
    <fails_when>
      - A non-DRAFT DELETE does not 400 (guard missing or wrong status comparison).
      - The existing DRAFT-delete success case fails (over-restriction).
      - The suite reports anything but PASS=70 FAIL=0.
      - docs/api/assessments.md Delete section still describes unconditional deletion.
    </fails_when>
    <human-check>
      Confirm the output of the 3 guard cases + the DRAFT-delete regression case in the recorded suite log (VERIFICATION gap 5's human-check: DELETE is refused for non-DRAFT states).
    </human-check>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Phase docs closure — project-status.md + tasks.md + final full-suite gate (AGENTS.md Rules 3/4)</name>
  <files>
    docs/project-status.md
    docs/tasks.md
  </files>
  <read_first>
    docs/project-status.md (current phase/feature/WIP/pending sections per AGENTS.md Rule 3)
    docs/tasks.md (Phase 08 goal tree — mark 08-05..08-07 items)
    .planning/phases/08-quiz-examination-management/08-VERIFICATION.md (the 5 gaps — the closure wording must cite them as resolved)
  </read_first>
  <action>
    1. docs/project-status.md — set the Phase 08 entry to a gated-closed posture:
       - Status: gap closure run (08-05..08-07) complete; 5 of 5 VERIFICATION gaps resolved (WR-01..WR-05); WR-06 explicitly deferred to Phase 9.
       - Completed: publish-gate ACTIVE filter + addQuestions ARCHIVED-block (08-05); merged-schedule re-validation + DTO title/questionIds validation (08-06); sortOrder max-offset + data resync + DRAFT-only DELETE guard (08-07).
       - Known issues: WR-06 (answer-key exposure) deferred to Phase 9 — one-line reason.
       - Validation: E2E PASS=70 FAIL=0 as of 2026-09-05; `pnpm typecheck && pnpm lint` green.
       - Recommended next task: run `/gsd-verify-phase 08` or execute Phase 9 planning — per orchestrator, not auto-continued (Rule 10).
    2. docs/tasks.md — mark every Phase 08 gap-closure item [x]: the 08-05..08-07 task lines (add them if the tree predates the gap run), and the WR-06 deferral line as [-] with reason.
    3. FINAL GATE (the whole phase, not just this plan):
       - `cd apps/api && pnpm typecheck && pnpm lint` — green.
       - `bash /tmp/opencode/p8_e2e.sh 2>&1 | tail -5` — PASS=70 FAIL=0.
       - `git status --porcelain packages/database` — NO schema/migration changes beyond the new resync script (schema-gate held: no db:generate output, no drizzle migration files).
       - `grep -c "\[ \]\|\[!\]" docs/user-validation.md` — 0 (file fully green, per Rule 12).
    Record all four results verbatim in the task summary (this is the phase-closure checkpoint evidence).
    Do NOT commit, do NOT push, do NOT create new issue entries beyond the marker updates — commit is the orchestrator's next step after verification.
  </action>
  <acceptance_criteria>
    - docs/project-status.md Phase 08 section reflects the gap-closure completion + WR-06 deferral + PASS=70 validation + recommended next task.
    - docs/tasks.md Phase 08 gap items all [x] (or [-] for WR-06 with reason).
    - Final gate: typecheck/lint green, suite PASS=70 FAIL=0, packages/database git diff contains ONLY the resync SQL, user-validation.md has 0 [ ]/[!] markers.
  </acceptance_criteria>
  <done>Phase 08 docs are in the AGENTS.md-compliant closed state; every gap-closure item and validation result is recorded; the final gate shows a green, schema-stable, fully-validated suite at PASS=70.</done>
  <verify>
    <automated>
      cd apps/api && pnpm typecheck && pnpm lint
      bash /tmp/opencode/p8_e2e.sh 2>&1 | tail -5   # PASS=70 FAIL=0
      git status --porcelain packages/database       # only the new resync script + schema files untouched
      grep -c "\[ \]\|\[!\]" docs/user-validation.md   # 0
    </automated>
    <fails_when>
      - typecheck/lint not green.
      - Suite not at PASS=70 FAIL=0.
      - packages/database shows drizzle migration files or schema diffs (schema-gate violation).
      - docs/user-validation.md has any unchecked marker.
    </fails_when>
  </verify>
</task>

</tasks>

### Artifacts this plan produces (gap-closure scope)

- `ExaminationsService.addQuestions` — single max(sortOrder) read per assessment, `base + i + 1` offsets
- `packages/database/scripts/resync-assessment-sort-order.sql` — tracked, idempotent, DDL-free data repair
- `ExaminationsService.deleteAssessment` — DRAFT-only status guard (400 otherwise)
- `docs/api/assessments.md` — Delete section DRAFT-only rule
- `/tmp/opencode/p8_e2e.sh` — 5 new gap cases → suite PASS=70
- `docs/user-validation.md` — EXAM-02/05 gap checks, all green
- `docs/project-status.md`, `docs/tasks.md` — phase-closure state per AGENTS.md Rules 3/4

### Assumptions recorded (assumption-delta contribution)

- **A-08-G6 (WR-05 — DELIBERATE, `costly`):** the DRAFT-only delete guard ships now. Rated **costly** per the reversibility taxonomy: no migration rode along the status-varchar check, BUT reverting means coordinated service+docs+E2E removal, and archived/published assessments cannot be un-deleted if a bad actor (or future bug) bypasses the guard — this is intentionally flagged, not silently accepted. Alternative recorded and REJECTED: defer the guard to Phase 9 with WR-06. Rejection reasoning: unconditional DELETE is the last open destructive write path; the guard is one `if` + throw, not a design commitment.
- **A-08-G7 (WR-04):** resync is a tracked SQL script run via psql (no tsx runner exists in apps/api — a TS script would have added tooling). Idempotent by construction; re-runs re-number identically.
- **A-08-G8:** E2E case counts are pinned (08-05: 60, 08-06: 65, 08-07: 70) — any deviation must adjust the fails_when numbers, never silently.

<threat_model>
**ASVS level 1 — block on HIGH.**

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API (delete) | the DELETE route is the destructive write boundary; the status guard decides killability server-side |
| client→API (link questions) | sortOrder originates server-side from DB max — clients supply only questionIds, never offsets |
| API→PostgreSQL | resync SQL + aggregate queries are parameterized/inspection-only; the script is a tracked, reviewed file |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-25 | Tampering | deleteAssessment non-DRAFT bypass | high | mitigate | status guard before the delete statements (WR-05); E2E proves PUBLISHED/ACTIVE/COMPLETED → 400 and DRAFT → success |
| T-08-26 | Tampering | sortOrder duplication from stale max reads | high | mitigate | single in-transaction `max(sortOrder)` per assessment, offsets `base + i + 1` (WR-04); data resync + 0-duplicate-group verification query |
| T-08-27 | Information Disclosure | resync script contents leak assessment ids | low | accept | ids originate from the tracked 08-VERIFICATION.md; the script is repo-internal, low-value outside the org |
| T-08-28 | Elevation of Privilege | DELETE guard bypass via race (status flips after check) | low | mitigate | status read + delete run inside the same request flow; assessments flip state via the service's own transition methods, sequential per request — full serialization is deferred (documented ponytail ceiling) |
| T-08-SC | Tampering | npm/pip/cargo installs | low | accept | No installs in this plan; dependency surface unchanged |

</threat_model>

<verification>
- `pnpm typecheck && pnpm lint` green after Tasks 1 and 2.
- Data invariant: duplicate-group query → 0 rows; contiguity check per assessment → max = count = count(DISTINCT).
- E2E: PASS=65 baseline reproduced, then PASS=67 (Task 1), then PASS=70 FAIL=0 (Task 2).
- Schema-gate: `git status --porcelain packages/database` shows only the new resync script — no migrations, no schema diffs.
- docs/user-validation.md: 0 unchecked markers.
</verification>

<success_criteria>
- The two known assessments are repaired (0 duplicate sortOrder groups) and future appends cannot re-create duplicates (max-offset computation).
- No non-DRAFT assessment can be deleted; the guard, its E2E cases, and its docs are in place (costly-decision note recorded in the summary for the orchestrator's awareness).
- Phase 08 docs are closed per AGENTS.md; the suite is green at PASS=70 FAIL=0 with a schema-stable tree.
</success_criteria>

<output>
Create `.planning/phases/08-quiz-examination-management/08-07-SUMMARY.md` when done
</output>