---
phase: 06-question-bank
plan: 01
title: Question schema, migration, contracts, and create/get/list tracer slice
status: planned
date: 2026-09-02
type: execute
wave: 1
autonomous: true
depends_on: []
files_modified:
  - packages/database/src/schema/questions.ts (new)
  - packages/database/src/schema/index.ts
  - packages/database/drizzle/0007_*.sql (generated)
  - packages/contracts/src/index.ts
  - apps/api/src/questions/questions.module.ts (new)
  - apps/api/src/questions/questions.controller.ts (new)
  - apps/api/src/questions/questions.service.ts (new)
  - apps/api/src/questions/dto/question.dto.ts (new)
  - apps/api/src/app/app.module.ts
  - docs/api/questions.md (new)
requirements: [QBN-01, QBN-03, QBN-04, QBN-06, QBN-07]
requirements_addressed: [QBN-01, QBN-03, QBN-04, QBN-06, QBN-07]

estimate:
  tokens: 52000
  raw_tokens: 26000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - The questions table exists in PostgreSQL with the exactly-one-scope CHECK (migration 0007 generated and applied cleanly).
    - POST /api/v1/questions with source MANUAL returns 201 and approvalStatus APPROVED (QBN-03/06); with source AI_GENERATED returns 201 and approvalStatus PENDING (QBN-07) — status computed server-side, never from the body.
    - GET /api/v1/questions/:questionId returns 200 for an in-institute id, 404 for a foreign/nonexistent id (tenant-scoped, anti-IDOR).
    - GET /api/v1/questions returns the full array for the institute (no pagination).
    - Request bodies containing approvalStatus or instituteId are rejected with 400 (whitelist mass-assignment hardening).
    - pnpm typecheck && pnpm lint pass; docs/api/questions.md documents the full locked API surface.
  artifacts:
    - packages/database/src/schema/questions.ts
    - packages/database/drizzle/0007_*.sql
    - packages/contracts/src/index.ts (Question contracts section)
    - apps/api/src/questions/questions.module.ts
    - apps/api/src/questions/questions.controller.ts
    - apps/api/src/questions/questions.service.ts
    - apps/api/src/questions/dto/question.dto.ts
    - docs/api/questions.md
  key_links:
    - Migration 0007 must stay in sync with the questions schema (no drizzle-kit push; generate + migrate only).
    - Every service query must AND eq(questions.instituteId, instituteId) — the anti-IDOR invariant.
    - Contract enum values and DTO @IsIn lists must match exactly (Pitfall 4: field-name drift caused Phase 5 400s).
    - Membership fixture status must be lowercase 'active' (Pitfall 3) or TenantGuard rejects with 403.
---

<objective>
Create the `questions` module's foundation: the Drizzle schema, its migration
(0007), the canonical Zod contracts, and the thinnest end-to-end slice — create
question (with the QBN-06/07 approval rule), retrieve by id, list. Approved and
AI-generated questions both flow through the same insert path so Phase 7's
worker slots in unchanged. Also writes the API contract doc (docs/api/questions.md)
against the RESEARCH-locked surface (contract-first, repo treats docs/api/ as
source of truth).

Purpose: Prove the full question stack end-to-end on one slice before expanding
to update/delete/filter/actions in 06-02. This phase's zero-dependency, clone-the
-content-module design makes the tracer the highest-information task.
Output: Working questions table + migration + create/get/list endpoints verified
against the live dockerized stack, plus the API contract doc.
</objective>

<execution_context>
@/home/ashif/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/ashif/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-question-bank/06-RESEARCH.md
@.planning/phases/06-question-bank/06-PATTERNS.md
AGENTS.md checkpoint protocol (docs/tasks.md, docs/project-status.md updates happen in 06-03; do not duplicate)
</context>

### Multi-Source Coverage Audit (Plan 06-01)

| Source item | Where covered |
|-------------|---------------|
| GOAL: question CRUD works; approval rules enforced | T1 (create/get/list+rule), 06-02 (update/delete/filter/actions), 06-03 (E2E close) |
| REQ QBN-01 create/list/retrieve | T1 |
| REQ QBN-03 manual creation | T1 (POST, WRITE_ROLES) |
| REQ QBN-04 explanation + source MANUAL\|AI_GENERATED | T1 (explanation column + source enum + response) |
| REQ QBN-06 manual auto-approved | T1 (insert rule) |
| REQ QBN-07 AI-generated begins PENDING | T1 (insert rule, same code path) |
| RESEARCH: zero deps, clone content module, varchar enums, JSONB payload + superRefine, exactly-one-scope CHECK, no pagination | T1 |
| RESEARCH: approvalStatus never accepted from body; DTO accepts both source values | T1 |
| RESEARCH: migration via generate+migrate (NOT drizzle-kit push), next id 0007 | T1 |
| CONTEXT: none exists (no discuss-phase) — no locked decisions, no deferred ideas | n/a — requirements + research govern |

Exclusions: AI question generation (Phase 7), examinations (Phase 8), frontend — all out of scope per REQUIREMENTS.md/ROADMAP.

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — questions schema + migration 0007 + contracts + create/get/list slice, verified end-to-end</name>
  <files>
    packages/database/src/schema/questions.ts (new)
    packages/database/src/schema/index.ts
    packages/database/drizzle/0007_*.sql (generated output)
    packages/contracts/src/index.ts
    apps/api/src/questions/dto/question.dto.ts (new)
    apps/api/src/questions/questions.module.ts (new)
    apps/api/src/questions/questions.controller.ts (new)
    apps/api/src/questions/questions.service.ts (new)
    apps/api/src/app/app.module.ts
  </files>
  <read_first>
    packages/database/src/schema/content.ts (schema skeleton + CHECK at :40-45)
    packages/database/src/schema/materials.ts (varchar enum column style at :29-38)
    packages/database/src/schema/index.ts (barrel — append one export)
    packages/database/drizzle.config.ts (schema glob + out dir; migrate via drizzle-kit, never push)
    packages/contracts/src/index.ts lines 174-379 (enum pattern, payload map + superRefine :313, AcademicScopeFields :295, scope refine :323, response schema :354)
    apps/api/src/content/content.controller.ts (guard stack, WRITE_ROLES, pipes, response wrapping)
    apps/api/src/content/content.service.ts (validatePayload :225, listContent :97, assertScopeInInstitute :254, resolveScope :238)
    apps/api/src/content/dto/content.dto.ts (DTO shape)
    apps/api/src/academic/academic.service.ts (tenant-scoped 404-on-miss get :51-63)
    apps/api/src/app/app.module.ts (module registration list)
    apps/api/src/identity/auth.controller.ts (register/login for E2E auth)
    infrastructure/compose/docker-compose.yml (migrate service :70-81, postgres creds)
    docs/user-validation.md:94-170 (Phase 5 fixtures to reuse: institute 11111111-..., subject slug 'math', membership casing note)
  </read_first>
  <action>
    Build the questions data+API slice. Do NOT use pgEnum (varchar is repo convention) and do NOT use drizzle-kit push — schema changes ship as generated migrations only (research [BLOCKING] requirement).

    1. Create packages/database/src/schema/questions.ts: pgTable('questions') with — id uuid pk defaultRandom; instituteId uuid notNull references institutes.id onDelete cascade; subjectId/chapterId/topicId uuid nullable each references the academic tables onDelete cascade (identical policy to content.ts:22-27); stem text notNull; questionType varchar(30) notNull; difficulty varchar(20) notNull default 'MEDIUM'; explanation text (nullable, no default); payload jsonb notNull; source varchar(20) notNull default 'MANUAL'; approvalStatus varchar(20) notNull default 'PENDING'; status varchar(20) notNull default 'ACTIVE'; createdBy uuid notNull references users.id; updatedBy uuid nullable references users.id; createdAt/updatedAt timestamp withTimezone notNull defaultNow. Table-level constraint: check 'questions_exactly_one_scope' with the exact SQL from content.ts:40-45 (subject/chapter/topic IS NOT NULL int-sum = 1).
    2. Append `export { questions } from './questions.js';` to packages/database/src/schema/index.ts (after the materials export line).
    3. Run `pnpm db:generate` (root script → drizzle-kit generate in packages/database). Validate a packages/database/drizzle/0007_*.sql file appeared and the journal advanced (0000..0006 exist; next is 0007). Then apply it to catlium_postgres — do NOT run drizzle-kit push; use the compose migrate service (`docker compose -f infrastructure/compose/docker-compose.yml run --rm migrate`). The migration must contain CREATE TABLE "questions" plus the questions_exactly_one_scope CHECK and the FK constraints (drizzle emits them; verify, never hand-edit the SQL).
    4. Append a Question contracts section to packages/contracts/src/index.ts (mirror the Content section at :174-379): QuestionTypeEnum (MCQ | TRUE_FALSE | FILL_IN_BLANK); QuestionDifficultyEnum (EASY | MEDIUM | HARD — values same as FlashcardDifficultyEnum at :225); QuestionSourceEnum (MANUAL | AI_GENERATED — exactly two values, Phase 7 worker compatibility); QuestionApprovalStatusEnum (PENDING | APPROVED | REJECTED); payload schemas MCQ (choices: array of {id: z.string().uuid(), text: z.string().min(1).max(1000)}, min 2 entries, correctChoiceId z.string().uuid()), TRUE_FALSE (correctAnswer z.boolean()), FILL_IN_BLANK (acceptableAnswers: z.array(z.string().min(1).max(500)).min(1)); QuestionPayloadSchemas map keyed by the three type values; CreateQuestionRequestSchema = {stem: z.string().min(1).max(20000), questionType, difficulty optional, explanation z.string().max(20000) optional, source: QuestionSourceEnum, payload: z.record(z.string(), z.unknown()), ...AcademicScopeFields} + superRefine dispatching on value.questionType through QuestionPayloadSchemas (path ['payload']) + refine exactly-one-of subjectId/chapterId/topicId (message "Exactly one of subjectId, chapterId, topicId must be provided", path ['scope']); UpdateQuestionRequestSchema = optional stem/difficulty/explanation/payload only (NO source/approvalStatus/questionType/scope fields — PATCH cannot change them); QuestionResponseSchema with id, instituteId, nullable scope ids, stem, questionType, difficulty, nullable explanation, payload record, source, approvalStatus, status, createdBy, updatedBy nullable, createdAt/updatedAt datetime; export QuestionListItemSchema as the same shape (no version nesting). Export all zod types (z.infer) alongside each schema — repo convention.
    5. Create apps/api/src/questions/dto/question.dto.ts: type QuestionType/QuestionDifficulty/QuestionSource unions matching the enums; CreateQuestionDto with stems @IsString @MaxLength(20000), questionType @IsIn(['MCQ','TRUE_FALSE','FILL_IN_BLANK']), difficulty @IsOptional @IsIn(['EASY','MEDIUM','HARD']), explanation @IsOptional @IsString @MaxLength(20000), payload @IsObject, source @IsIn(['MANUAL','AI_GENERATED']), subjectId/chapterId/topicId @IsOptional @IsUUID. CRITICAL: no approvalStatus field and no instituteId field — the global ValidationPipe (whitelist + forbidNonWhitelisted, main.ts:20-22) turns any attempt to send them into a 400 (mass-assignment hardening; per QBN-05 actions and research Patterns.md).
    6. Create apps/api/src/questions/questions.module.ts: clone academic.module.ts verbatim shape, but for QuestionsController + QuestionsService (no imports array — DatabaseModule is @Global(); do NOT import JobsModule, Phase 6 has no async work).
    7. Create apps/api/src/questions/questions.controller.ts: const WRITE_ROLES = ['INSTITUTE_ADMIN', 'TEACHER'] as const; @Controller('questions'); @UseGuards(AccessTokenGuard, TenantGuard, RolesGuard) (imports from ../common/guards and ../common/decorators exactly as content.controller.ts:17-26). Endpoints this task: POST / @HttpCode(HttpStatus.CREATED) @RequiredRoles(...WRITE_ROLES) create(@Tenant(), @CurrentUser(), @Body() CreateQuestionDto) → return { question } (createdBy from user.userId per content.controller.ts:38-42); GET /:questionId with ParseUUIDPipe → get → { question }; GET / list(@Tenant()) → { questions } with NO filter params yet (filters land in 06-02 — do not add them here). Question param name: questionId everywhere for consistency.
    8. Create apps/api/src/questions/questions.service.ts (clone content.service.ts anatomy): constructor @Inject(DATABASE_TOKEN) db; CreateQuestionInput interface (stem, questionType, difficulty?, explanation?, source, payload, subjectId?/chapterId?/topicId?); createQuestion(instituteId, createdBy, input) — validatePayload(input.questionType, input.payload) FIRST, then resolveScope(input) (copy content.service.ts:238-252), then assertScopeInInstitute(instituteId, kind, id) (copy content.service.ts:254-291 verbatim — subject direct check, chapter one-hop join, topic two-hop join, NotFoundException each), then insert with values: instituteId, the single scope column (others null), stem, questionType, difficulty: input.difficulty ?? 'MEDIUM', explanation: input.explanation ?? null, payload, source, approvalStatus computed server-side as input.source === 'MANUAL' ? 'APPROVED' : 'PENDING' (QBN-06/QBN-07 — the ONLY place approval status is ever set on create; never read from the DTO), status: 'ACTIVE', createdBy, updatedBy: createdBy, .returning(). getQuestion(instituteId, questionId) — where(and(eq(questions.id, questionId), eq(questions.instituteId, instituteId))).limit(1) + NotFoundException('Question not found') (exact academic.service.ts:51-63 shape). listQuestions(instituteId) — select from questions where eq(instituteId) orderBy desc(updatedAt) (content.service.ts:97-111 with the institute condition only). private validatePayload(questionType, payload) — QuestionPayloadSchemas[questionType].safeParse, throw BadRequestException(`Invalid ${questionType} payload: ${path} — ${message}`) on failure (content.service.ts:225-236 shape). No 409/unique-violation mapping needed this phase: the questions table has no unique constraints — do NOT copy isUniqueViolation in.
    9. Register QuestionsModule in apps/api/src/app/app.module.ts: add the import line and `QuestionsModule,` to imports after MaterialsModule (lines 11, 39).
    10. Commit checkpoint per AGENTS.md Rule 8 after verification passes (feat(questions): ...).
  </action>
  <acceptance_criteria>
    - packages/database/src/schema/questions.ts exists with the 18 listed columns, the three varchar enum columns are NOT pgEnum, and the questions_exactly_one_scope CHECK is in the table definition.
    - packages/database/drizzle/0007_*.sql exists (generated, not hand-written), the journal meta/_journal.json lists its tag, and the SQL contains CREATE TABLE "questions", the exactly-one-scope CHECK, and the FK constraints.
    - Migration applied cleanly: `\d questions` (or information_schema query) shows the table and the CHECK constraint name on catlium_postgres/catlium_dev.
    - pnpm typecheck && pnpm lint pass across the workspace.
    - POST /api/v1/questions with {source: "MANUAL"} and a valid MCQ/topic payload → 201, response body question.approvalStatus === "APPROVED", question.source === "MANUAL", question.status === "ACTIVE".
    - POST /api/v1/questions with {source: "AI_GENERATED"} and a valid TRUE_FALSE payload → 201, question.approvalStatus === "PENDING" (QBN-07).
    - GET /api/v1/questions/:questionId → 200 with the created question; GET /api/v1/questions → array containing it; GET with a random uuidv4 → 404; GET of institute-A's question id while scoped to institute B → 404 (anti-IDOR).
    - POST with approvalStatus or instituteId in the body → 400 (forbidNonWhitelisted).
    - A manual question created with an AI-generatable shape (source AI_GENERATED) landed PENDING, proving the Phase 7 worker insert path lands on PENDING unchanged.
  </acceptance_criteria>
  <verify>
    <automated>
      # 1. Static gates (run first; migration step requires clean schema)
      pnpm typecheck && pnpm lint
      # 2. Migration generate + apply + DB validation (BLOCKING — no drizzle-kit push anywhere)
      pnpm db:generate
      ls packages/database/drizzle/0007_*.sql
      grep -c 'questions_exactly_one_scope' packages/database/drizzle/0007_*.sql
      docker compose -f infrastructure/compose/docker-compose.yml run --rm migrate
      docker compose exec postgres psql -U catlium -d catlium_dev -tAc "SELECT constraint_name FROM information_schema.check_constraints WHERE constraint_name='questions_exactly_one_scope'"
      # 3. E2E against the live stack (API_PORT=3000; reuse Phase 5 fixtures)
      #    a) register a fresh teacher; bind membership (status MUST be lowercase 'active'; role INSTITUTE_ADMIN)
      #    b) SUBJECT_ID=$(docker compose exec -T postgres psql -U catlium -d catlium_dev -tAc "SELECT id FROM subjects WHERE slug='math'")
      #    c) POST MANUAL MCQ (choice ids = uuidgen -r) with x-institute-id 11111111-... → expect 201 + approvalStatus APPROVED
      #    d) POST AI_GENERATED TRUE_FALSE same scope → expect 201 + approvalStatus PENDING
      #    e) GET /questions/:id (both ids) → 200; GET /questions → array length >= 2; GET random uuid → 404
      #    f) anti-IDOR: user2 + institute B membership → GET institute-A question id with B header → 404
      #    g) mass-assignment: POST body with "approvalStatus":"REJECTED" → 400; with "instituteId":"<other>" → 400
      curl -s -b /tmp/opencode/q6.cookies -o /dev/null -w '%{http_code}' \
        -X POST http://localhost:3000/api/v1/questions \
        -H 'Content-Type: application/json' \
        -H 'x-institute-id: 11111111-1111-1111-1111-111111111111' \
        -d '{"stem":"What is 2+2?","questionType":"MCQ","difficulty":"EASY","explanation":"Basic addition","source":"MANUAL","topicId":"<topic-uuid>","payload":{"choices":[{"id":"<uuidgen>","text":"3"},{"id":"<uuidgen>","text":"4"}],"correctChoiceId":"<uuid-of-4>"}}'
    </automated>
    <fails_when>
      - pnpm db:generate exits non-zero or produces no 0007_*.sql file (schema not picked up → fix schema/index.ts barrel or questions.ts first).
      - The generated 0007 SQL lacks the questions_exactly_one_scope CHECK (drizzle emitted a malformed table → fix schema, regenerate).
      - The migrate command errors against catlium_postgres, or the information_schema query returns zero rows (migration not applied).
      - Any curl response other than: c) 201 + approvalStatus APPROVED; d) 201 + approvalStatus PENDING; e) 200/200/404; f) 404; g) 400 body-statuses — a wrong status code or a leaked cross-institute row fails the task.
      - pnpm typecheck or pnpm lint reports errors in the new files.
    </fails_when>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Write the questions API contract doc (docs/api/questions.md)</name>
  <files>docs/api/questions.md (new)</files>
  <read_first>
    docs/api/content.md (full anatomy: header/auth note/error format, entity JSON sketch, payload contracts, create/list/get/update/action sections)
    06-RESEARCH.md "Key design decisions" and Assumptions A2/A4/A6 (surface locks)
    06-PATTERNS.md "docs/api/questions.md" row (:478-491)
  </read_first>
  <action>
    Write docs/api/questions.md mirroring docs/api/content.md's anatomy, documenting the FULL RESEARCH-locked surface for all 10 endpoints (create, list, get, update, delete, approve, reject, archive, activate) — including endpoints implemented in 06-02; this doc is the contract those tasks implement against (repo: docs/api/ is the source of truth, roadmap line 5). Sections required:
    - Header + auth note (cookie access_token + x-institute-id + membership 'active'; roles INSTITUTE_ADMIN/TEACHER for all writes) + error format (400/401/403/404/409 per global-exception.filter) — copy content.md style.
    - Entity JSON sketch of a Question: field names EXACTLY as CreateQuestionDto/QuestionResponseSchema name them (stem, questionType, difficulty, explanation, payload, source, approvalStatus, status, subjectId/chapterId/topicId, createdBy/updatedBy, createdAt/updatedAt) — Pitfall 4: payloads in this doc become the user-validation payloads, name fields from the contract, NOT from memory.
    - Payload contracts section: MCQ / TRUE_FALSE / FILL_IN_BLANK shapes (choice ids must be uuidv4 strings — Pitfall 2 note for Phase 7) with the zod-canonical note like content.md.
    - Endpoint sections for all 10: POST /api/v1/questions (201; source MANUAL → approvalStatus APPROVED, AI_GENERATED → PENDING — state the QBN-06/07 rule verbatim as an explicit sentence), GET /api/v1/questions (with the six query filters and their enum values — mirrors content.md list section), GET /api/v1/questions/:questionId, PATCH /api/v1/questions/:questionId (field-limited; payload re-validated; approvalStatus/source/questionType/scope immutable), DELETE /api/v1/questions/:questionId (204 NO CONTENT, institute-scoped, cross-institute → 404 — the FIRST 204 documented anywhere in docs/api, call it out), POST /:questionId/approve, /reject (approval transitions; REJECTED→APPROVED and APPROVED→REJECTED both allowed), /archive, /activate.
    - Explicit negative section: approvalStatus or instituteId in any request body → 400 (never client-settable).
  </action>
  <acceptance_criteria>
    - docs/api/questions.md exists and contains a section for each of the 10 endpoints with the exact paths above.
    - It contains the QBN-06/07 rule as prose (create with source MANUAL → APPROVED; AI_GENERATED → PENDING).
    - It contains a DELETE section stating 204 + cross-institute 404.
    - It states that approvalStatus/instituteId are rejected from request bodies.
    - It uses the exact field names from the contracts (grep: no 'code' field, no 'approvalStatus' listed in any REQUEST body).
  </acceptance_criteria>
  <verify>
    <automated>
      grep -q 'POST /api/v1/questions' docs/api/questions.md
      grep -q 'DELETE /api/v1/questions/:questionId' docs/api/questions.md
      grep -q 'GET /api/v1/questions' docs/api/questions.md
      grep -q 'approvalStatus: "APPROVED"' docs/api/questions.md
      grep -qE 'PENDING' docs/api/questions.md
      grep -c '204' docs/api/questions.md | grep -v '^0$'
      grep -qE 'never (accepted|client-settable)' docs/api/questions.md
    </automated>
    <fails_when>
      - Any grep above returns nothing (missing endpoint/rule/204/never-accepted statements → doc is incomplete).
      - The doc lists approvalStatus or instituteId inside any request body example (would contradict the mass-assignment hardening the code enforces).
    </fails_when>
  </verify>
</task>

</tasks>

### Artifacts this phase produces (Plan 06-01 scope; full phase covers 06-02/06-03)

- `questions` Drizzle table — `packages/database/src/schema/questions.ts` (id, instituteId, subjectId, chapterId, topicId, stem, questionType, difficulty, explanation, payload, source, approvalStatus, status, createdBy, updatedBy, createdAt, updatedAt) + CHECK `questions_exactly_one_scope`; barrel export in `packages/database/src/schema/index.ts`
- Migration `packages/database/drizzle/0007_*.sql` + journal entry
- Contracts in `packages/contracts/src/index.ts`: `QuestionTypeEnum`, `QuestionDifficultyEnum`, `QuestionSourceEnum`, `QuestionApprovalStatusEnum` (+ matching exported types), `McqPayloadSchema`, `TrueFalsePayloadSchema`, `FillInBlankPayloadSchema`, `QuestionPayloadSchemas`, `CreateQuestionRequestSchema`, `UpdateQuestionRequestSchema`, `QuestionResponseSchema`, `QuestionListItemSchema`
- `apps/api/src/questions/`: `QuestionsModule` (questions.module.ts), `QuestionsController` (questions.controller.ts — POST /api/v1/questions, GET /api/v1/questions, GET /api/v1/questions/:questionId), `QuestionsService` (questions.service.ts — createQuestion, getQuestion, listQuestions, validatePayload, resolveScope, assertScopeInInstitute), `CreateQuestionDto` (dto/question.dto.ts)
- `apps/api/src/app/app.module.ts` — QuestionsModule registered
- `docs/api/questions.md` — 10-endpoint contract doc

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API (CREATE/READ) | unauthenticated/unscoped or role-deficient HTTP traffic crosses here; x-institute-id header is untrusted until TenantGuard validates membership |
| API→PostgreSQL | DTO-derived values become SQL insert values here; instituteId scope and approvalStatus must be server-computed by this point |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Information Disclosure | GET /questions/:questionId (`getQuestion`) | high | mitigate | Tenant-scoped where `and(eq(questions.id, questionId), eq(questions.instituteId, instituteId))` + NotFoundException on miss (academic.service.ts:51-63 pattern); E2E asserts institute-B-scoped GET of institute-A id → 404 — this is the first read surface in the module, never query by id alone |
| T-06-02 | Tampering | POST /questions body (`CreateQuestionDto`) | high | mitigate | DTO has NO approvalStatus/instituteId fields; global ValidationPipe forbidNonWhitelisted turns smuggled fields into 400; approvalStatus computed server-side from source (QBN-06/07), instituteId injected from @Tenant() only |
| T-06-03 | Elevation of Privilege | POST /questions (student creates a question) | high | mitigate | @RequiredRoles(...WRITE_ROLES) = INSTITUTE_ADMIN/TEACHER on the create handler + RolesGuard + TenantGuard active-membership check (tenant.guard.ts:42, lowercase 'active') |
| T-06-04 | Tampering | payload JSONB at create | medium | mitigate | superRefine dispatch on questionType via QuestionPayloadSchemas in CreateQuestionRequestSchema AND validatePayload() in the service — 400 on type/shape mismatch before any insert |
| T-06-05 | Information Disclosure | GET /questions list | medium | mitigate | listQuestions always ANDs eq(questions.instituteId, instituteId) as the first condition; list is scoped even before 06-02 adds filters |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | ZERO new packages this phase (verified in 06-RESEARCH.md Package Legitimacy Audit); existing deps already pinned+audited; no install command appears in any task |
</threat_model>

<verification>
- `pnpm typecheck && pnpm lint` green.
- Migration 0007 exists, contains the CHECK + FKs, applies cleanly to catlium_postgres/catlium_dev (psql proof).
- E2E curl flow from Task 1 verify passes: 201/APPROVED, 201/PENDING, 200/200/404 reads, cross-institute 404, mass-assignment 400s.
- Contract doc grep gates pass.
</verification>

<success_criteria>
- A teacher can create a manual MCQ question and immediately retrieve it APPROVED; an AI_GENERATED question lands PENDING through the identical code path Phase 7 will reuse.
- The questions table is live in PostgreSQL with the exactly-one-scope invariant enforced at the DB level.
- No request can set approvalStatus or instituteId; no query can cross institutes.
- docs/api/questions.md states the full contract the rest of the phase implements.
</success_criteria>

<output>
Create `.planning/phases/06-question-bank/06-01-SUMMARY.md` when done
</output>