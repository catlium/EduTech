---
phase: 06-question-bank
plan: 02
title: Full question CRUD completion — update, delete, filtering, approval actions
status: planned
date: 2026-09-02
type: execute
wave: 2
autonomous: true
depends_on: ["06-01"]
files_modified:
  - apps/api/src/questions/questions.controller.ts
  - apps/api/src/questions/questions.service.ts
  - apps/api/src/questions/dto/question.dto.ts
requirements: [QBN-01, QBN-02, QBN-05]
requirements_addressed: [QBN-01, QBN-02, QBN-05]

estimate:
  tokens: 42000
  raw_tokens: 21000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - PATCH /api/v1/questions/:questionId updates stem/difficulty/explanation/payload only (whitelist blocks questionType/source/approvalStatus/scope → 400) and re-validates payload shape (malformed payload → 400).
    - DELETE /api/v1/questions/:questionId hard-deletes institute-scoped and returns 204; foreign-institute id → 404 (first @Delete in the platform, anti-IDOR).
    - GET /api/v1/questions filters by questionType, difficulty, approvalStatus, subjectId, chapterId, topicId (QBN-02) with AND semantics; invalid enum value → 400.
    - POST /questions/:id/approve and /reject move approvalStatus to APPROVED/REJECTED; POST /archive and /activate move status to ARCHIVED/ACTIVE; all institute-scoped with 404 on miss (QBN-05 reachable pre-Phase-7).
    - STUDENT members are denied every write/action (403); reads remain allowed (200).
  artifacts:
    - questions.service.ts: updateQuestion, deleteQuestion, setApprovalStatus, setStatus
    - questions.controller.ts: PATCH, DELETE (204), approve, reject, archive, activate handlers + list filters
    - dto/question.dto.ts: UpdateQuestionDto
  key_links:
    - validatePayload must run on PATCH payloads too (Pitfall 6 — E2E asserts a 1-choice MCQ patch → 400).
    - The DELETE handler has NO in-repo precedent — the tenant-scoped where + 404 is copied from getSubject; skipping eq(instituteId) is the cross-tenant leak.
    - approve/reject must be WRITE_ROLES-gated and institute-scoped; a student reaching them would be a privilege escalation.
---

<objective>
Complete the question CRUD surface and make every approval state reachable:
PATCH update (field-limited, payload re-validated), the platform's first DELETE,
QBN-02 list filtering (difficulty, question type, subject/chapter/topic), and the
approve/reject/archive/activate action endpoints. This makes PENDING/REJECTED
testable before Phase 7 and closes QBN-01/QBN-02/QBN-05.

Purpose: Expand the proven tracer slice into the full requirement surface without
introducing any new architectural pattern — every addition clones an existing
content/academic counterpart.
Output: Complete, secured question CRUD + filtering + approval workflow actions,
verified E2E against the live stack.
</objective>

<execution_context>
@/home/ashif/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/ashif/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-question-bank/06-RESEARCH.md
@.planning/phases/06-question-bank/06-PATTERNS.md
Prior plan: `.planning/phases/06-question-bank/06-01-SUMMARY.md` (schema/contracts/service/controller built there — this plan extends those files; if the SUMMARY is missing, read 06-01-PLAN.md Task 1 for the symbols this plan extends)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PATCH update (field-limited, payload re-validated) + DELETE (first platform 204)</name>
  <files>
    apps/api/src/questions/dto/question.dto.ts
    apps/api/src/questions/questions.controller.ts
    apps/api/src/questions/questions.service.ts
  </files>
  <read_first>
    apps/api/src/content/content.service.ts:132-177 (update: locked select → validatePayload → update → returning)
    apps/api/src/academic/academic.service.ts:79-94 (update pattern) and :51-63 (404-on-miss tenant-scoped predicate — the DELETE derives from this)
    apps/api/src/academic/academic.controller.ts:62-71 (PATCH handler shape — DELETE derives from it)
    .planning/phases/06-question-bank/06-RESEARCH.md Pitfall 5 (first DELETE without tenant scope = cross-institute leak) and Pitfall 6 (payload unchecked on PATCH)
  </read_first>
  <behavior>
    - Test 1: PATCH {stem} on an in-institute question → 200 with updated stem and refreshed updatedAt; approvalStatus/source/questionType unchanged.
    - Test 2: PATCH with a 1-choice MCQ payload → 400 (payload re-validated on update).
    - Test 3: PATCH body containing approvalStatus or questionType or subjectId → 400 (whitelist).
    - Test 4: DELETE in-institute question → 204, empty body; subsequent GET → 404.
    - Test 5: DELETE with institute-B scope on an institute-A question id → 404 (anti-IDOR on the platform's first delete).
    - Test 6: PATCH/DELETE with a random uuidv4 → 404.
  </behavior>
  <action>
    Add UpdateQuestionDto to apps/api/src/questions/dto/question.dto.ts: all fields @IsOptional — stem @IsString @MaxLength(20000), difficulty @IsIn(['EASY','MEDIUM','HARD']), explanation @IsString @MaxLength(20000), payload @IsObject. NO questionType, NO source, NO approvalStatus, NO scope-id fields — PATCH can never change type/source/approval/scope (whitelist → 400; research "PATCH is field-limited").

    In QuestionsService:
    - updateQuestion(instituteId, userId, questionId, patch): first getQuestion(instituteId, questionId) (exists + tenant-owned); if patch.payload !== undefined, run this.validatePayload(existing.questionType, patch.payload) (Pitfall 6 — re-validate on update, the shared helper from 06-01); then update(questions).set({ ...patch, updatedBy: userId, updatedAt: new Date() }).where(and(eq(questions.id, questionId), eq(questions.instituteId, instituteId))).returning(); NotFoundException('Question not found') if no row (academic.service.ts:79-94 shape).
    - deleteQuestion(instituteId, questionId): delete(questions).where(and(eq(questions.id, questionId), eq(questions.instituteId, instituteId))).returning(); if no row → NotFoundException('Question not found'). FIRST @Delete in the platform — without the instituteId predicate a deletion leaks cross-tenant (Pitfall 5).

    In QuestionsController:
    - @Patch(':questionId') @RequiredRoles(...WRITE_ROLES): update(@Tenant(), @CurrentUser(), @Param('questionId', ParseUUIDPipe), @Body() UpdateQuestionDto) → { question }.
    - @Delete(':questionId') @RequiredRoles(...WRITE_ROLES) @HttpCode(HttpStatus.NO_CONTENT): delete(@Tenant(), @Param('questionId', ParseUUIDPipe)) → await service.deleteQuestion(...) and return nothing (204, no body). Import Delete from @nestjs/common — zero existing @Delete in apps/api/src (verified); this is the first.
  </action>
  <acceptance_criteria>
    - PATCH stem/difficulty/explanation on the tracer's MANUAL question → 200, stem/difficulty replaced, approvalStatus still APPROVED, questionType unchanged.
    - PATCH with MCQ payload of exactly 1 choice → 400 (validatePayload on update).
    - PATCH body with approvalStatus:"APPROVED" → 400; with questionType or subjectId → 400.
    - DELETE that same question → 204 with empty body; GET after → 404.
    - DELETE/PATCH with institute-B scope on institute-A question id → 404.
    - All mutation handlers reject student (403) — covered in Task 3's sweep; Task 1 verifies role gate via the shared WRITE_ROLES constant presence.
  </acceptance_criteria>
  <verify>
    <automated>
      pnpm typecheck && pnpm lint
      # E2E (continues 06-01 fixtures: teacher cookie + x-institute-id 11111111-...)
      # PATCH happy path: expect 200, new stem, approvalStatus still APPROVED
      curl -s -b /tmp/opencode/q6.cookies -X PATCH http://localhost:3000/api/v1/questions/<qid> \
        -H 'Content-Type: application/json' -H 'x-institute-id: 11111111-1111-1111-1111-111111111111' \
        -d '{"stem":"Updated stem"}'
      # PATCH malformed MCQ payload (1 choice): expect 400
      # PATCH with approvalStatus / questionType / subjectId in body: expect 400
      # DELETE: expect 204; subsequent GET: expect 404; random-uuid DELETE: expect 404
      # anti-IDOR: institute-B member PATCH/DELETE of institute-A question: expect 404
    </automated>
    <fails_when>
      - PATCH returns 200 while approvalStatus/questionType changed, or returns 200 for a malformed 1-choice MCQ payload (payload gate skipped on update), or returns 200 for a body containing approvalStatus/questionType/subjectId (whitelist bypass).
      - DELETE returns anything other than 204 for an in-institute id, or returns 204/200 for an institute-B-scoped id (tenant leak), or returns a body.
      - pnpm typecheck/lint fail.
    </fails_when>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: List filtering — questionType, difficulty, approvalStatus, subject/chapter/topic (QBN-02)</name>
  <files>
    apps/api/src/questions/questions.controller.ts
    apps/api/src/questions/questions.service.ts
  </files>
  <read_first>
    apps/api/src/content/content.controller.ts:51-76 (ParseEnumPipe optional + ParseUUIDPipe optional query filters — copy verbatim semantics)
    apps/api/src/content/content.service.ts:97-111 (conditions array, institute condition always first)
    .planning/phases/06-question-bank/06-RESEARCH.md (filters: typed columns, not payload keys)
  </read_first>
  <behavior>
    - Test 1: GET /questions?questionType=MCQ → only MCQ rows (mix of MCQ/TF/FITB present beforehand).
    - Test 2: GET /questions?difficulty=EASY → only EASY rows.
    - Test 3: GET /questions?approvalStatus=APPROVED → only APPROVED rows.
    - Test 4: GET /questions?subjectId=<math> → only rows scoped to that subject (and institute).
    - Test 5: GET /questions?questionType=MCQ&approvalStatus=APPROVED → AND semantics.
    - Test 6: GET /questions?difficulty=INSANE → 400 (ParseEnumPipe).
    - Test 7: GET /questions?subjectId=not-a-uuid → 400 (ParseUUIDPipe).
  </behavior>
  <action>
    Service — change listQuestions signature to listQuestions(instituteId, filters: ListQuestionFilters) where ListQuestionFilters = { questionType?, difficulty?, approvalStatus?, subjectId?, chapterId?, topicId? } with the enum/string union types (type unions from 06-01's contracts or local unions matching the DTO file). Build conditions: start with [eq(questions.instituteId, instituteId)] (tenant isolation mandate — always first), then push eq for each provided filter (questions.questionType, questions.difficulty, questions.approvalStatus, questions.subjectId, questions.chapterId, questions.topicId), orderBy desc(updatedAt) — exact content.service.ts:97-111 anatomy.

    Controller — extend the existing GET / handler with six optional query params, copied from content.controller.ts:51-76: questionType via new ParseEnumPipe(['MCQ','TRUE_FALSE','FILL_IN_BLANK'], { optional: true }); difficulty via ParseEnumPipe(['EASY','MEDIUM','HARD'], { optional: true }); approvalStatus via ParseEnumPipe(['PENDING','APPROVED','REJECTED'], { optional: true }); subjectId/chapterId/topicId via new ParseUUIDPipe({ optional: true }). Pass through to service.listQuestions(tenant.instituteId, { questionType, difficulty, approvalStatus, subjectId, chapterId, topicId }); return { questions }.
  </action>
  <acceptance_criteria>
    - GET /questions?questionType=MCQ returns only MCQ rows from the institute; same for difficulty and approvalStatus filters.
    - GET /questions?subjectId=<math-subject-id> returns only rows whose subjectId matches (and rows scoped to chapter/topic of that subject do NOT appear — the filter is on the exact column, matching content precedent).
    - Combined filters AND correctly.
    - Invalid enum (difficulty=INSANE) and malformed uuid params → 400 via ParseEnumPipe/ParseUUIDPipe.
  </acceptance_criteria>
  <verify>
    <automated>
      pnpm typecheck && pnpm lint
      # E2E: ensure >=3 questions exist across types/difficulties (create TRUE_FALSE EASY PENDING + FITB HARD APPROVED if needed)
      # GET /questions?questionType=MCQ  → all rows questionType MCQ
      # GET /questions?difficulty=EASY   → all rows difficulty EASY
      # GET /questions?approvalStatus=APPROVED → all rows APPROVED
      # GET /questions?subjectId=<math-uuid>  → all rows subjectId == math-uuid
      # GET /questions?questionType=MCQ&approvalStatus=APPROVED → intersection
      # GET /questions?difficulty=INSANE → 400 ; GET /questions?subjectId=not-a-uuid → 400
      # verify no row leaks: each filtered response only ever contains institute-A questions
    </automated>
    <fails_when>
      - A filtered list returns rows not matching the filter (wrong WHERE), or returns another institute's rows (tenant condition missing/not first).
      - difficulty=INSANE or subjectId=not-a-uuid return 200 instead of 400.
      - Combined filters return the union instead of the intersection.
    </fails_when>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Approve/reject/archive/activate action endpoints + security verification sweep</name>
  <files>
    apps/api/src/questions/questions.controller.ts
    apps/api/src/questions/questions.service.ts
  </files>
  <read_first>
    apps/api/src/content/content.controller.ts:120-138 (archive/activate action handlers — literal template)
    apps/api/src/content/content.service.ts:209-221 (setStatus update+404 — literal template for setApprovalStatus and setStatus)
    .planning/phases/06-question-bank/06-RESEARCH.md (approve/reject rationale: QBN-05 reachability before Phase 7; transitions REJECTED→APPROVED and APPROVED→REJECTED allowed)
  </read_first>
  <behavior>
    - Test 1: POST /questions/:id/reject on the PENDING AI_GENERATED question → 200 approvalStatus REJECTED.
    - Test 2: POST /questions/:id/approve on that same question → 200 approvalStatus APPROVED (REJECTED→APPROVED allowed).
    - Test 3: POST /questions/:id/approve on the APPROVED MANUAL question → 200 still APPROVED (idempotent).
    - Test 4: POST /questions/:id/archive → status ARCHIVED; /activate → ACTIVE.
    - Test 5: STUDENT membership: all mutate handlers (create/PATCH/DELETE/approve/reject/archive) → 403; GET list/get → 200.
    - Test 6: institute-B member acting on institute-A question (approve/reject/archive) → 404.
    - Test 7: random-uuid action → 404.
  </behavior>
  <action>
    Service — add two methods cloned from content.service.ts setStatus (:209-221):
    - setApprovalStatus(instituteId, questionId, approvalStatus: 'APPROVED' | 'REJECTED'): update(questions).set({ approvalStatus, updatedAt: new Date() }).where(and(eq id, eq instituteId)).returning(); NotFoundException on miss. No state-machine beyond the two allowed values — REJECTED→APPROVED and APPROVED→REJECTED both permitted (research: review re-opens allowed).
    - setStatus(instituteId, questionId, status: 'ACTIVE' | 'ARCHIVED'): same shape (content precedent; makes the status column reachable — research recommends archive/activate for lifecycle consistency).
    Type unions for approvalStatus/status live locally in the service (or import from 06-01's contracts).

    Controller — four action handlers cloned verbatim in shape from content.controller.ts:120-138, all @RequiredRoles(...WRITE_ROLES):
    - POST ':questionId/approve' → service.setApprovalStatus(tenant.instituteId, questionId, 'APPROVED') → { question }
    - POST ':questionId/reject' → setApprovalStatus(..., 'REJECTED') → { question }
    - POST ':questionId/archive' → setStatus(..., 'ARCHIVED') → { question }
    - POST ':questionId/activate' → setStatus(..., 'ACTIVE') → { question }

    Security verification sweep (run and record outcomes in the task summary):
    1. Register a STUDENT user, bind a membership to institute A with role 'STUDENT' (status lowercase 'active' — Pitfall 3; SQL inserts via docker compose exec postgres psql).
    2. With the student cookie: POST /questions → 403; PATCH → 403; DELETE → 403; POST approve/reject/archive → 403; GET list and GET :id → 200 (reads allowed, writes denied).
    3. Register a second teacher, bind to institute B (SQL: institutes insert + membership + INSTITUTE_ADMIN role). With B's header: GET/PATCH/DELETE/approve on institute-A question id → 404 every time (anti-IDOR; no existence oracle).
    4. Confirm no exception filter blasts: all these errors come back in the global { statusCode, message, error } shape.
  </action>
  <acceptance_criteria>
    - Reject then approve the PENDING AI_GENERATED question → REJECTED then APPROVED (both directions work).
    - approve/archive/activate return { question } with the new status value; random-uuid and institute-B-scoped calls → 404.
    - Student sees 403 on every mutation handler and 200 on reads.
    - Cross-institute actions never return 2xx.
    - pnpm typecheck && pnpm lint pass.
  </acceptance_criteria>
  <verify>
    <automated>
      pnpm typecheck && pnpm lint
      # E2E (student + institute-B fixtures from the sweep; teacher cookie for the happy paths)
      # reject PENDING question: expect 200 approvalStatus REJECTED
      # approve same: expect 200 approvalStatus APPROVED
      # archive then activate: expect 200 status ARCHIVED then ACTIVE
      # student: POST/PATCH/DELETE/approve/archive → 403 each; GET /questions → 200
      # institute-B member on institute-A question: GET/PATCH/DELETE/approve → 404 each
      # random-uuid approve/archive → 404
      # every non-2xx body matches { statusCode, message, error } shape
    </automated>
    <fails_when>
      - approve/reject/archive/activate return non-200 for in-institute ids, or a wrong approvalStatus/status value.
      - Any student mutation returns 200/201 (privilege escalation — RolesGuard bypassed).
      - Any institute-B-scoped action on an institute-A question returns 2xx (tenant leak on the action endpoints).
      - Error responses deviate from the global exception filter shape.
    </fails_when>
  </verify>
</task>

</tasks>

### Artifacts this phase produces (Plan 06-02 scope)

- `QuestionsService` additions (`apps/api/src/questions/questions.service.ts`): `updateQuestion`, `deleteQuestion` (platform's first hard-delete service method), `setApprovalStatus`, `setStatus`, `ListQuestionFilters` interface
- `QuestionsController` additions (`apps/api/src/questions/questions.controller.ts`): `@Patch(':questionId')`, `@Delete(':questionId')` (204), `@Post(':questionId/approve'|'reject'|'archive'|'activate')`, list query filters (questionType/difficulty/approvalStatus via ParseEnumPipe, subjectId/chapterId/topicId via ParseUUIDPipe); `Delete` import — first `@Delete` anywhere in `apps/api/src`
- `UpdateQuestionDto` (`apps/api/src/questions/dto/question.dto.ts`)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API (UPDATE/DELETE/ACTIONS) | mutation traffic crosses here; the DELETE handler is the platform's first — no precedent exists to copy a sloppy version from |
| API→PostgreSQL | PATCH set-values and DELETE predicates become SQL here; delete/update predicates MUST carry instituteId |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-06 | Tampering | DELETE /questions/:questionId (`deleteQuestion`) | high | mitigate | The platform's first @Delete: predicate is `and(eq(questions.id, questionId), eq(questions.instituteId, instituteId))` + NotFoundException on miss (derived from getSubject, academic.service.ts:51-63); E2E asserts institute-B delete of institute-A question → 404. No where(id)-only delete path exists. |
| T-06-07 | Elevation of Privilege | all mutation handlers (PATCH/DELETE/approve/reject/archive/activate) | high | mitigate | Every handler carries @RequiredRoles(...WRITE_ROLES) (INSTITUTE_ADMIN/TEACHER) on top of AccessTokenGuard+TenantGuard+RolesGuard; student sweep asserts 403 on all six, 200 on reads — role enforcement is per-handler, not a single shared gate, so each handler must declare it |
| T-06-08 | Tampering | PATCH /questions/:questionId body + payload | medium | mitigate | UpdateQuestionDto whitelists stem/difficulty/explanation/payload only — approvalStatus/source/questionType/scope-id fields absent → 400; validatePayload(existing.questionType, patch.payload) runs on every PATCH with payload (Pitfall 6) |
| T-06-09 | Tampering | approve/reject workflow abuse | high | mitigate | setApprovalStatus is WRITE_ROLES-gated and institute-scoped (404 cross-institute); only APPROVED questions are usable downstream, enforced at Phase 8 EXAM-08 — this phase guarantees APPROVED/REJECTED are reachable and transition only through these role-gated actions, never the create/PATCH body |
| T-06-10 | Information Disclosure | filtered list | medium | mitigate | listQuestions conditions array starts with eq(questions.instituteId, instituteId); every added filter is AND-ed (content.service.ts:97-111) — filters narrow within the institute, they never widen |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | No installs in this plan; existing deps audited in 06-RESEARCH.md; no new package enters the lockfile |
</threat_model>

<verification>
- `pnpm typecheck && pnpm lint` green.
- E2E: update/delete/filters/actions all behave per acceptance criteria; security sweep (student 403s, institute-B 404s, error-shape check) recorded in the task 3 summary.
- No 2xx cross-institute result; no student mutation succeeds.
</verification>

<success_criteria>
- Question bank CRUD is complete: create (06-01), read/list/filter, update, delete — and every approval state (PENDING/APPROVED/REJECTED) is reachable through role-gated, institute-scoped actions.
- The first DELETE ships with the same tenant-scoped 404 discipline as every read.
- A student cannot mutate any question; a member of another institute cannot touch this institute's questions by guessing ids.
</success_criteria>

<output>
Create `.planning/phases/06-question-bank/06-02-SUMMARY.md` when done
</output>