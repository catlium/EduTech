---
phase: 06-question-bank
plan: 03
title: E2E validation checklist, task tracking docs, and phase close
status: planned
date: 2026-09-02
type: execute
wave: 3
autonomous: true
depends_on: ["06-02"]
files_modified:
  - docs/user-validation.md
  - docs/tasks.md
  - docs/project-status.md
requirements: [QBN-01, QBN-02, QBN-03, QBN-04, QBN-05, QBN-06, QBN-07]
requirements_addressed: [QBN-01, QBN-02, QBN-03, QBN-04, QBN-05, QBN-06, QBN-07]

estimate:
  tokens: 30000
  raw_tokens: 15000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - docs/user-validation.md has a Phase 6 section covering QBN-01..07 (setup + per-req tests + negative/security cases) and every item is marked [x] — the E2E run passed against the live stack.
    - docs/api/questions.md is verified against implemented behavior (CON-01-style sweep; discrepancies fixed, not hidden — Phase 5's doc-said-404/API-said-403 precedent).
    - docs/tasks.md contains the Phase 6 block with all QBN-01..07 items [x]; docs/project-status.md has the Phase 6 checkpoint entry per AGENTS.md Rules 3/4.
    - Final pnpm typecheck && pnpm lint pass; the phase's meaningful checkpoint is committed.
  artifacts:
    - docs/user-validation.md (Phase 6 section)
    - docs/tasks.md (Phase 6 block)
    - docs/project-status.md (Phase 6 entry)
  key_links:
    - Checklist payloads must be named from the zod contract/DTO (Pitfall 4 — Phase 5's slug-vs-code drift cost a full re-run).
    - Fixture membership status stays lowercase 'active' (Pitfall 3).
---

<objective>
Close the phase with the repo's validation architecture: the E2E checklist in
docs/user-validation.md (platform standard — zero automated tests exist by
design), all items run and marked [x] against the dockerized stack; the AGENTS.md
checkpoint protocol docs (docs/tasks.md, docs/project-status.md); and a
contract-vs-behavior sweep of docs/api/questions.md (CON-01 style, Phase 5
precedent: the 404-vs-403 doc discrepancy caught at close time).

Purpose: Every QBN requirement gets a runnable validation trail, and a fresh
session can recover the phase state from docs alone (AGENTS.md Rule 6-7).
Output: Closed phase with a committed checkpoint and all validation items green.
</objective>

<execution_context>
@/home/ashif/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/ashif/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-question-bank/06-RESEARCH.md (Validation Architecture section :407-438 — the test map is the checklist skeleton)
Prior plan SUMMARYs: `.planning/phases/06-question-bank/06-01-SUMMARY.md`, `06-02-SUMMARY.md` (implemented surface + sweep results this plan documents; read before writing the checklist)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the Phase 6 E2E checklist in docs/user-validation.md and run it to all [x]</name>
  <files>docs/user-validation.md</files>
  <read_first>
    docs/user-validation.md:1-13 (item format) and :94-170 (Phase 5 prereqs/fixtures to reuse: institute 11111111-..., membership 22222222-... lowercase 'active', subject slug 'math', register/login endpoints)
    .planning/phases/06-question-bank/06-RESEARCH.md:419-433 (Phase Requirements → Test Map: the seven QBN behaviors + their E2E checks)
    .planning/phases/06-question-bank/06-RESEARCH.md Pitfalls 3/4/5 (casing, field-name drift, tenant-scoped delete)
    .planning/phases/06-question-bank/06-01-SUMMARY.md and 06-02-SUMMARY.md (actual endpoints + behavior to encode)
  </read_first>
  <action>
    Append a "## Phase 6 — Question Bank (E2E)" section to docs/user-validation.md following the established format (:1-13, Phase 5 section at :16-292). Structure:
    - Setup prerequisites: reuse the live Phase 5 fixtures where present (institute 11111111-..., membership 22222222-... status lowercase 'active', academic scope from prereq C via `SELECT id FROM subjects WHERE slug='math'`); register fresh teacher + student users and bind memberships via SQL when needed. State the casing requirement explicitly (Pitfall 3).
    - One item per requirement, fields exactly as the format prescribes (Setup required / Endpoint / Payload / Expected output), payloads named VERBATIM from the CreateQuestionDto/contract field names (Pitfall 4: use stem/questionType/difficulty/explanation/payload/source/subjectId — never invented names):
      - QBN-01: create → 201; list → array; retrieve → 200; PATCH → 200; DELETE → 204 then GET → 404.
      - QBN-02: filter tests per questionType/difficulty/approvalStatus/subjectId/chapterId/topicId + invalid-enum 400.
      - QBN-03: STUDENT create → 403 (manual creation is teacher-only).
      - QBN-04: create with explanation + source MANUAL → response echoes both.
      - QBN-05: reject PENDING → REJECTED; approve → APPROVED; filter by approvalStatus.
      - QBN-06: manual create → approvalStatus APPROVED in the 201 response.
      - QBN-07: create with source AI_GENERATED → approvalStatus PENDING in the 201 response.
      - Security/negative block: approvalStatus/instituteId in body → 400; cross-institute (institute-B member) GET/PATCH/DELETE/approve of institute-A question → 404; random uuid → 404; no cookie → 401; non-member institute header → 403 (Phase 5 finding: TenantGuard 403, NOT 404 — encode the CORRECT expectation, see user-validation.md:65-69).
    - Run every item against the live dockerized stack (docker compose ps first; up --build if down) and mark each [x] with a note of the run date. Any [!] must be diagnosed and the code/docs fixed before the item can flip to [x] — no deferred items allowed for a phase close (per Conventions :294-300).
    - If any behavior contradicts docs/api/questions.md, do NOT silently edit the contract doc here — record the discrepancy in the task 3 note so Task 2's sweep fixes it deliberately (CON-02: "Do not silently change the API contract; resolve inconsistencies deliberately").
  </action>
  <acceptance_criteria>
    - docs/user-validation.md contains "## Phase 6 — Question Bank (E2E)" with items covering QBN-01..07 plus the security/negative block.
    - Every Phase 6 item is marked [x]; the section header carries a passing run date.
    - The checklist's payload field names match the contract exactly (no 'code'/'approvalStatus-in-body' drift).
    - Any doc-vs-behavior discrepancy found is logged for Task 2.
  </acceptance_criteria>
  <verify>
    <automated>
      pnpm typecheck
      # all phase-6 items checked (no [ ], no [!] remain inside the Phase 6 section)
      awk '/## Phase 6 — Question Bank/,0' docs/user-validation.md | grep -v '^#' | grep -cE '\[ \]|\[!\]' | grep -w 0
      # key items exist
      grep -q 'source AI_GENERATED' docs/user-validation.md
      grep -q 'approvalStatus APPROVED' docs/user-validation.md
      grep -q 'STUDENT' docs/user-validation.md
    </automated>
    <fails_when>
      - awk-grep counts any [ ] or [!] marker in the Phase 6 section (item not run or failed — the phase cannot close with an unchecked item).
      - The QBN-06/07 tests are absent from the section (approval rule never validated).
      - typecheck fails.
    </fails_when>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Update docs/tasks.md + docs/project-status.md, sweep docs/api/questions.md vs behavior, final gate, commit checkpoint</name>
  <files>
    docs/tasks.md
    docs/project-status.md
    docs/api/questions.md (only if the Task 1 discrepancy log found mismatches)
  </files>
  <read_first>
    docs/tasks.md (existing Phase 5 block — same format: Phase → Goal → task checkboxes with markers [ ]/[~]/[x]/[-]/[!])
    docs/project-status.md (existing checkpoint format — AGENTS.md Rule 3: current phase, status, completed, WIP, pending, deferred, decisions, DB changes, validation status, latest checkpoint, recommended next task)
    .planning/phases/06-question-bank/06-01-PLAN.md/06-02-PLAN.md verification sections (what each task proved, for the validation-status line)
  </read_first>
  <action>
    1. docs/tasks.md — add the "Phase 6 — Question Bank" block per AGENTS.md Rule 4 structure (Phase → Goal → Tasks) listing the seven QBN requirements as checkboxes and mark each [x]; keep Phase 5's statuses untouched.
    2. docs/project-status.md — add/replace the Phase 6 entry per AGENTS.md Rule 3: status "COMPLETE (E2E validated 2026-09-02)", completed work (questions module, migration 0007, contracts, 10 endpoints, approval workflow), pending (Phase 7), deferred (none), DB changes (questions table + exactly-one-scope CHECK), validation status (all user-validation Phase 6 items [x]), latest checkpoint (the commit from this task), recommended next task (plan Phase 7 — AI Question Generation & Review).
    3. Contract sweep (CON-01 style): re-read docs/api/questions.md from 06-01 and diff it against the ACTUAL implemented behavior (routes, status codes, field names, error semantics — pull from the 06-01/06-02 SUMMARYs and the user-validation run). Fix every mismatch in docs/api/questions.md deliberately and note each fix in the SUMMARY. Candidates, per Phase 5 precedent: 404-vs-403 semantics on wrong-institute header, exact response field names, error messages.
    4. Final gate: run `pnpm typecheck && pnpm lint` from the repo root; both green.
    5. Commit per AGENTS.md Rule 8/10: meaningful checkpoint covering the docs + any doc fixes (docs(phase6): close question bank phase with E2E validation — or feat(questions): ... if code fixes were required during Task 1). Push when a remote is available (Rule 9). Record the commit hash in docs/project-status.md if the format holds it.
  </action>
  <acceptance_criteria>
    - docs/tasks.md Phase 6 block lists QBN-01..07 all marked [x]; other phases' entries unmodified.
    - docs/project-status.md Phase 6 entry exists with all Rule-3 fields (status, completed, DB changes, validation status, latest checkpoint, recommended next task).
    - docs/api/questions.md matches implemented behavior after the sweep — no knowingly-wrong statement left.
    - pnpm typecheck && pnpm lint pass; checkpoint committed (and pushed if a remote exists).
  </acceptance_criteria>
  <verify>
    <automated>
      pnpm typecheck && pnpm lint
      # tasks.md Phase 6 block: all seven QBN items checked
      awk '/Phase 6 — Question Bank/,0' docs/tasks.md | grep -v '^#' | grep -cE '\[ \]|\[~\]|\[!\]|\[-\]' | grep -w 0
      # project-status has the Phase 6 entry with validation evidence
      grep -q 'QBN-01' docs/tasks.md
      grep -q 'COMPLETE' docs/project-status.md
      # phase-6 user-validation section still fully green after any edits
      awk '/## Phase 6 — Question Bank/,0' docs/user-validation.md | grep -v '^#' | grep -cE '\[ \]|\[!\]' | grep -w 0
      git log --oneline -1
    </automated>
    <fails_when>
      - Any QBN item in tasks.md is not [x], or project-status lacks the Phase 6 entry.
      - typecheck/lint fail.
      - The user-validation Phase 6 section regressed (an awk count above 0).
      - No new commit exists for the phase close (git log -1 is a pre-phase commit).
    </fails_when>
  </verify>
</task>

</tasks>

### Artifacts this phase produces (Plan 06-03 scope)

- `docs/user-validation.md` — "## Phase 6 — Question Bank (E2E)" section (QBN-01..07 items + security/negative block, all [x])
- `docs/tasks.md` — Phase 6 block (QBN-01..07 [x])
- `docs/project-status.md` — Phase 6 COMPLETE entry (Rule-3 fields)
- `docs/api/questions.md` — verified/corrected to match implemented behavior

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → future sessions/Phase 15 verification | wrong documentation becomes wrong contracts; a doc claiming approvalStatus is client-settable actively undermines the security posture |
| checklist → phase-close | an unchecked [!] item closed anyway hides a real defect (Phase 5's doc-vs-API 403/404 drift was only caught at E2E close) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-11 | Repudiation | docs/user-validation.md phase close | medium | mitigate | Phase 6 section may only close with every item [x]; awk gate fails on any [ ]/[!]; run is date-stamped in the section header (Phase 5 precedent 2026-09-02) |
| T-06-12 | Tampering | docs/api/questions.md contract integrity | medium | mitigate | CON-01-style sweep in Task 2 diffs the doc against implemented routes/status codes/field names; discrepancies are fixed deliberately and logged (CON-02), never silently edited during Task 1 |
| T-06-13 | Information Disclosure | checklist credentials/fixtures | low | accept | The doc only carries dummy fixture UUIDs + throwaway test accounts (established Phase 5 pattern); no real credentials or tokens are recorded |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | No installs in this plan; dependency surface unchanged from 06-01/06-02 (audited) |
</threat_model>

<verification>
- Phase 6 user-validation section fully [x]; tasks.md QBN block fully [x]; project-status Phase 6 COMPLETE entry present.
- docs/api/questions.md swept against behavior with fixes logged.
- Final `pnpm typecheck && pnpm lint` green; checkpoint committed (+ pushed when remote exists).
</verification>

<success_criteria>
- Every QBN-01..07 requirement has a passed, dated E2E validation item in docs/user-validation.md — the phase's observable proof.
- A fresh session can recover Phase 6 state and the exact next task (Phase 7 planning) from docs alone.
- Phase 6 is closed per AGENTS.md checkpoint protocol with the commit pushed.
</success_criteria>

<output>
Create `.planning/phases/06-question-bank/06-03-SUMMARY.md` when done
</output>